# Codex Log

This file is Codex's own work log for the Claude+Codex workflow, used to record Codex-specific actions, decisions, and completed tasks in a concise running history that can be extended over time.

## Template

### YYYY-MM-DD HH:MM TZ
- Task:
- Changes:
- Notes:

## Entries

### 2026-06-24 08:34 IST
- Task: Created `docs/Codex-Log.md`.
- Changes: Added the header, a reusable template section, and the initial log entry.
- Notes: Created as the starting work log for the Claude+Codex workflow.

### 2026-06-24 08:35 IST
- Task: Redesigned PO Create across the frontend page and procurement PO API handlers.
- Changes: Moved Payment Term and Freight Term from header-level inputs to each material line, added a per-line drawer for Remarks and Rebate details (rate, basis, remarks), added a header-level drawer for GST Terms and repeatable Extra Fields, and relabeled the per-line delivery date field to `ETD` for Domestic POs or `ETA to Port` for Import POs.
- Notes: Files touched were `frontend/src/pages/dashboard/procurement/po/POCreatePage.jsx` and `supabase/functions/api/_core/procurement/po.handlers.ts`.

### 2026-06-24 12:27 IST
- Task: Reworked PO create line details and materials extras UI in the frontend page.
- Changes: Moved GST Terms from the header-level drawer into each line's `LineMoreDrawer`, added per-line `gst_terms` state and payload mapping, removed the header `More Details` flow entirely, added a `+ Add Field` Materials action, and rendered `extra_fields` inline under the materials grid with per-row remove controls.
- Notes: Edited only `frontend/src/pages/dashboard/procurement/po/POCreatePage.jsx` and appended this log entry.

### 2026-06-24 12:32 IST
- Task: Updated PO create backend GST term handling to validate per material instead of at the header/group level.
- Changes: Removed top-level `gst_terms` parsing and validation from `createPOHandler`, stopped writing `gst_terms` onto `erp_procurement.po_order_group`, and added optional per-material validation from `materialRecord.gst_terms` inside the materials loop while leaving the `purchase_order` insert unchanged.
- Notes: A migration adding a `gst_terms` column to `erp_procurement.purchase_order` is still required before the per-line value can be persisted; right now the handler validates `materialRecord.gst_terms` and then silently drops it.

### 2026-06-24 12:45 IST
- Task: Added reusable Procurement user display-name resolution and applied it to PO and PO-order-group responses/UI.
- Changes: Created `supabase/functions/api/_shared/resolveUserDisplayNames.ts`, updated `supabase/functions/api/_core/procurement/po.handlers.ts` to append `*_by_display` siblings across returned PO, PO line, PO order-group, approval-log, and amendment-log payloads via one batched resolver pass per response, and updated `frontend/src/pages/dashboard/procurement/po/POListPage.jsx` to render `created_by_display` with fallback to the raw UUID.
- Notes: Files touched were `supabase/functions/api/_shared/resolveUserDisplayNames.ts`, `supabase/functions/api/_core/procurement/po.handlers.ts`, `frontend/src/pages/dashboard/procurement/po/POListPage.jsx`, and `docs/Codex-Log.md`.

### 2026-06-24 15:33 IST
- Task: Replaced raw PO master UUIDs with readable company and line-item display values in procurement PO detail/list responses and the PO detail UI.
- Changes: Added batched PO reference lookups in `supabase/functions/api/_core/procurement/po.handlers.ts` so PO detail responses now include `company_name`, `material_display`, `cost_center_display`, and `payment_term_display`, extended the PO list response with `company_name`, updated `frontend/src/pages/dashboard/procurement/po/PODetailPage.jsx` to render the company name in the header, replaced the line material UUID cell with the formatted material display, and inserted Cost Center and Payment Term columns after Rate.
- Notes: Kept the lookup strategy to one query per master table across the returned lines to avoid N+1 queries and left existing fallbacks to raw IDs in place when a display value is missing.

### 2026-06-24 15:42 IST
- Task: Surfaced additional purchase-order header, rebate, and audit fields already present in the PO detail API response.
- Changes: Updated `frontend/src/pages/dashboard/procurement/po/PODetailPage.jsx` to add GST Terms, ETD/ETA, and Remarks to the Header card, inserted a conditional Rebate card for `has_rebate`, and added an Audit card showing resolved creator/updater/cancel/knock-off display names using `ErpFieldPreview`.
- Notes: Matched the ETD vs `ETA to Port` label to the existing PO create-page import logic by checking vendor type first and falling back to `po.delivery_type`.

### 2026-06-24 16:17 IST
- Task: Added DRAFT-only purchase-order editing on the procurement PO detail page.
- Changes: Updated `frontend/src/pages/dashboard/procurement/po/PODetailPage.jsx` to load payment terms and company-scoped cost centers, show an `Edit` action only for DRAFT POs, initialize a draft-edit modal from the loaded PO, and submit the backend-compatible single-line payload through `updatePurchaseOrder()` before reloading the detail view.
- Notes: Tried to run `frontend` lint on the touched file, but the local Node/npm process was blocked by a filesystem `EPERM` on `C:\Users\cpalm`, so this change was verified by code inspection only.

### 2026-06-24 17:49 IST
- Task: Completed TanStack Query migration section 1 for Procurement PO detail.
- Changes: Updated `frontend/src/pages/dashboard/procurement/po/PODetailPage.jsx` to replace local vendors, payment terms, cost centers, PO detail, and CSN fetch state/effects with shared query hooks plus page-specific `useQuery` calls keyed by PO id/company context so remount and back-navigation reuse cache while preserving existing edit, amendment, and audit behavior.
- Notes: Verified with `npm.cmd run build` in `frontend/` after the refactor.

### 2026-06-24 18:33 IST
- Task: Completed TanStack Query migration section 2 for the remaining Procurement screens and shared query foundation files.
- Changes: Added the shared React Query foundation and query-hook convention in `frontend/package.json`, `frontend/package-lock.json`, `frontend/src/main.jsx`, `frontend/src/hooks/queries/README.md`, `frontend/src/hooks/queries/queryClient.js`, `frontend/src/hooks/queries/queryKeys.js`, `frontend/src/hooks/queries/queryUtils.js`, `frontend/src/hooks/queries/useHrMasterQueries.js`, `frontend/src/hooks/queries/useOmMasterQueries.js`, and `frontend/src/hooks/queries/useProcurementMasterQueries.js`; migrated the remaining Procurement screens to shared master-data hooks plus page-level `useQuery` caching in `frontend/src/pages/dashboard/procurement/accounts/IVDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/accounts/LandedCostListPage.jsx`, `frontend/src/pages/dashboard/procurement/gate/GateEntryCreatePage.jsx`, `frontend/src/pages/dashboard/procurement/grn/GRNDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/inventory/PIDocumentDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/CHAMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/DomesticLeadTimeMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/ImportLeadTimeMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/MaterialCategoryMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/PaymentTermsMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/PortTransitMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/masters/TransporterMasterPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockListPage.jsx`, `frontend/src/pages/dashboard/procurement/po/POOrderGroupDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/qa/QADocumentPage.jsx`, `frontend/src/pages/dashboard/procurement/qa/QAQueuePage.jsx`, `frontend/src/pages/dashboard/procurement/rtv/DebitNoteDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/rtv/DebitNoteListPage.jsx`, `frontend/src/pages/dashboard/procurement/rtv/ExchangeRefListPage.jsx`, `frontend/src/pages/dashboard/procurement/rtv/RTVCreatePage.jsx`, `frontend/src/pages/dashboard/procurement/rtv/RTVDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/sales/SOCreatePage.jsx`, `frontend/src/pages/dashboard/procurement/sales/SODetailPage.jsx`, `frontend/src/pages/dashboard/procurement/sales/SalesInvoiceDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/sto/STOCreatePage.jsx`, and `frontend/src/pages/dashboard/procurement/sto/STODetailPage.jsx`.
- Notes: Verified with `npm.cmd run build` in `frontend/` after the refactor; procurement grep targets for duplicated `listVendors|listCostCenters|listPaymentTerms|listCompanies|listMaterials` fetches now resolve through the shared query hooks.

### 2026-06-24 19:06 IST
- Task: Completed TanStack Query migration section 3 for the Operation Management module.
- Changes: Migrated OM list, create, and detail screens to shared master-data query hooks plus page-level `useQuery` caching in `frontend/src/pages/dashboard/om/asl/AslCreatePage.jsx`, `frontend/src/pages/dashboard/om/asl/AslDetailPage.jsx`, `frontend/src/pages/dashboard/om/asl/AslListPage.jsx`, `frontend/src/pages/dashboard/om/customer/CustomerCreatePage.jsx`, `frontend/src/pages/dashboard/om/customer/CustomerDetailPage.jsx`, `frontend/src/pages/dashboard/om/customer/CustomerListPage.jsx`, `frontend/src/pages/dashboard/om/material/MaterialCreatePage.jsx`, `frontend/src/pages/dashboard/om/material/MaterialDetailPage.jsx`, `frontend/src/pages/dashboard/om/material/MaterialListPage.jsx`, `frontend/src/pages/dashboard/om/vendor/VendorDetailPage.jsx`, and `frontend/src/pages/dashboard/om/vendor/VendorListPage.jsx`.
- Notes: Verified with `npm.cmd run build` in `frontend/`; OM master lookups now flow through the shared query-hook pattern and detail/list remounts reuse cached query data for back-navigation.

### 2026-06-24 19:37 IST
- Task: Completed TanStack Query migration section 4 for the HR module.
- Changes: Extended the shared HR query layer in `frontend/src/hooks/queries/queryKeys.js` and `frontend/src/hooks/queries/useHrMasterQueries.js`, migrated shared HR workspaces and report screens in `frontend/src/pages/dashboard/hr/HrRegisterReports.jsx` and `frontend/src/pages/dashboard/hr/HrWorkflowPages.jsx`, and converted attendance/correction/report pages to shared hooks plus page-level query caching in `frontend/src/pages/dashboard/hr/attendance/HrAttendanceCorrectionPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrCorrectionApprovalHistoryPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrCorrectionApprovalInboxPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrCorrectionPendingListPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrCorrectionRequestDetailPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrDailyAttendanceRegisterPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrDepartmentAttendanceReportPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrLeaveUsageReportPage.jsx`, `frontend/src/pages/dashboard/hr/attendance/HrMonthlyAttendanceSummaryPage.jsx`, and `frontend/src/pages/dashboard/hr/attendance/HrYearlyLeaveSummaryPage.jsx`.
- Notes: Verified with `npm.cmd run build` in `frontend/`; HR master/reference lists and request/register/report datasets now resolve through the shared query pattern for cache reuse and faster remounts.

### 2026-06-24 20:15 IST (Claude)
- Task: Completed TanStack Query migration section 5 (SA admin screens) — finishing and verifying Codex's uncommitted section 5 work.
- Changes: Verified Codex's migration of SACostCenterMaster.jsx, SAMachineMaster.jsx, SAOmNumberSeries.jsx, SAOmUomMaster.jsx, SAVendorMaster.jsx (all correctly wired to shared hooks). Found and fixed a regression in SAOmStorageLocations.jsx: the diff had imported useStorageLocationsQuery/useAdminCompaniesQuery/useQueryClient but never called them, while loadRows()/loadMeta() still referenced the now-removed listStorageLocations import and a deleted BASE constant — both would throw ReferenceError on mount. Rewired loadRows/loadMeta to use the shared hooks plus queryClient.invalidateQueries on mutations, and surfaced query errors in the notices banner.
- Notes: Verified with `npx vite build` and `npx eslint` (project-wide) after the fix; the only other lint findings (`react-hooks/set-state-in-effect`) were confirmed pre-existing via `git show 428c138` and are unrelated to this migration. Committed as 688e185.

### 2026-06-24 21:02 IST
- Task: Added a separate Opening / Legacy Purchase Order entry flow for loading pre-PACE open and in-transit POs with manual PO numbers and no approval step.
- Changes: Updated `supabase/functions/api/_core/procurement/po.handlers.ts` so `createPOHandler` accepts `is_opening_po`, requires a manual `po_number` only for opening POs, skips auto-number generation for that branch, and persists `is_opening_po` on inserted purchase-order rows; created `frontend/src/pages/dashboard/procurement/po/POCreateOpeningPage.jsx` as a copy of the existing PO create flow with added `Legacy PO Number` and `PO Date` header inputs plus create-then-confirm submission logic that auto-confirms every returned PO and reports partial confirm failures; added the new screen registry entry in `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, routed the page in `frontend/src/router/AppRouter.jsx`, and added the create-opening companion detail allowance in `frontend/src/router/routeIndex.js`.
- Notes: Verified with `npm.cmd run build` in `frontend/` and a direct `tsx` import check of `supabase/functions/api/_core/procurement/po.handlers.ts`; left the existing normal PO create/list/detail flows untouched.

### 2026-06-25 10:14 IST
- Task: Fixed stale Purchase Order Order-Group status reconciliation after individual PO status changes, then removed the redundant Pending Approvals shortcut from the PO list page.
- Changes: Updated `supabase/functions/api/_core/procurement/po.handlers.ts` to add `syncOrderGroupStatus(groupId, actionedBy)`, which reloads all member POs via `getOrderGroupPOs(groupId)` and recomputes group status with this aggregation rule: all member POs `CANCELLED` -> group `CANCELLED`; else all member POs terminal and at least one confirmed-like (`CONFIRMED`, with `CLOSED` treated as confirmed-equivalent for post-confirm knock-off lifecycle rows) -> group `CONFIRMED`; else any member `PENDING_APPROVAL` -> group `PENDING_APPROVAL`; else group `DRAFT`. Wired that sync into individual PO status-changing handlers after their PO updates: `confirmPOHandler`, `approvePOHandler`, `rejectPOHandler`, `amendPOHandler`, `approveAmendmentHandler`, `cancelPOHandler`, `knockOffPOLineHandler`, and `knockOffPOHandler`. Updated `frontend/src/pages/dashboard/procurement/po/POListPage.jsx` to remove the in-page `Pending Approvals` shortcut action and its helper, leaving the dedicated PO13 menu entry as the single access point.
- Notes: Verified with `npm.cmd run build` in `frontend/` and a direct `tsx` import check of `supabase/functions/api/_core/procurement/po.handlers.ts`; logically, a single-PO group that starts `DRAFT` and is confirmed through `confirmPOHandler` now immediately reconciles to group status `CONFIRMED`.

### 2026-06-25 13:51 IST
- Task: Replaced the SA Material Master free-text category field with a material-type-scoped searchable dropdown plus inline category creation.
- Changes: Added backend handlers in `supabase/functions/api/_core/om/material_type_category.handlers.ts` and route wiring in `supabase/functions/api/_routes/om.routes.ts` for listing active `erp_master.material_type_category` rows by optional `material_type` and idempotently creating new rows; added `listMaterialTypeCategories()` and `createMaterialTypeCategory()` in `frontend/src/pages/dashboard/om/omApi.js`; extended `frontend/src/hooks/queries/queryKeys.js` and `frontend/src/hooks/queries/useOmMasterQueries.js` with shared material-type-category query hooks; updated `frontend/src/admin/sa/screens/SAMaterialMaster.jsx` so the Category column now uses a type-scoped `ErpComboboxField`, supports inline `+ Add new category…` creation, invalidates the shared query cache after create, preserves the text payload shape for bulk save/CSV import, and clears stale category text when a row’s material type changes.
- Notes: Verified with `npm.cmd run build` in `frontend/` and an escalated `tsx` import check of `supabase/functions/api/_core/om/material_type_category.handlers.ts` plus `supabase/functions/api/_routes/om.routes.ts`; this environment did not execute a live DB read/write against `erp_master.material_type_category`, so verification here confirms compile/import wiring rather than database contents.

### 2026-06-25 16:36 IST
- Task: Built the full STO approval workflow to parity with PO approvals, added sub-CSN sourcing for consignment-distribution STOs, and surfaced pending STOs in the shared Pending Approvals page.
- Changes: Updated `supabase/functions/api/_core/procurement/sto.handlers.ts` so `INTER_PLANT` STOs now create as `DRAFT`, added `confirmSTOHandler`, `approveSTOHandler`, and `rejectSTOHandler`, added per-line CSN creation on STO confirm/approve for `INTER_PLANT`, reused a shared consignment builder so `CONSIGNMENT_DISTRIBUTION` STO creation can accept multiple selected sub-CSNs without rewriting the transform flow, and made STO cancellation unlink any linked sub-CSNs; updated `supabase/functions/api/_core/procurement/csn.handlers.ts` so sub-CSN creation can persist `consignee_company_id` and added `listAvailableSubCsnsForStoHandler` to return open sub-CSNs scoped by sending company, receiving company, material, mother-PO validity, and unused `sto_id`; updated `supabase/functions/api/_core/procurement/po.handlers.ts` so `listPOOrderGroupsHandler` now merges pending/filtered STO rows into the same response shape as PO approval groups with `doc_type: "STO"`; wired the new backend routes in `supabase/functions/api/_routes/procurement.routes.ts` and added matching ACL entries in `supabase/functions/api/_acl/route-acl-registry.ts`; extended `frontend/src/pages/dashboard/procurement/procurementApi.js` with the new STO confirm/approve/reject and available-sub-CSN calls; updated `frontend/src/pages/dashboard/procurement/sto/STOCreatePage.jsx` so consignment lines can pick scoped sub-CSNs from an inline drawer and inter-plant STO creation immediately confirms into the approval flow; updated `frontend/src/pages/dashboard/procurement/sto/STODetailPage.jsx` with confirm-for-approval plus approve/reject actions; and updated `frontend/src/pages/dashboard/procurement/po/POOrderGroupListPage.jsx` so `doc_type: "STO"` rows open STO detail instead of PO order-group detail.
- Notes: Verified with `npm.cmd run build` in `frontend/` and an escalated `tsx` import check covering `supabase/functions/api/_core/procurement/sto.handlers.ts`, `supabase/functions/api/_core/procurement/csn.handlers.ts`, `supabase/functions/api/_core/procurement/po.handlers.ts`, `supabase/functions/api/_routes/procurement.routes.ts`, and `supabase/functions/api/_acl/route-acl-registry.ts`; explicitly checked that every new route added in `procurement.routes.ts` also received a matching entry in `route-acl-registry.ts`.

### 2026-06-25 17:05 IST
- Task: Added a separate Opening / Legacy STO entry flow for loading pre-PACE standalone stock transfer orders with manual STO numbers and immediate direct confirmation.
- Changes: Updated `supabase/functions/api/_core/procurement/sto.handlers.ts` so `createSTOHandler` now accepts `is_opening_sto`, rejects opening requests unless `sto_type` is `INTER_PLANT`, uses a caller-supplied manual `sto_number` instead of generated numbering for that branch, and persists `is_opening_sto` on the inserted STO while keeping the existing normal flow untouched; created `frontend/src/pages/dashboard/procurement/sto/STOCreateOpeningPage.jsx` as a dedicated legacy-only INTER_PLANT create screen with `Legacy STO Number`, `STO Date`, the same company/material/storage-location/transfer-price line structure as the normal STO page, and create-then-confirm submission using `confirmSTO(..., { approval_required: false })`; added the screen registry entry in `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, routed it in `frontend/src/router/AppRouter.jsx`, and added the companion-route allowance in `frontend/src/router/routeIndex.js`.
- Notes: Verified with `npm.cmd run build` in `frontend/` and an escalated `tsx` import check of `supabase/functions/api/_core/procurement/sto.handlers.ts`; this change is purely additive and does not modify the existing normal STO create/list/detail screens.

### 2026-06-25 18:02 IST
- Task: Rebuilt normal and opening STO create flows to mirror the PO create UX and payload structure, added last-used payment-term prefilling, and moved storage-location capture from create-time to dispatch-prep on STO detail.
- Changes: Updated `supabase/functions/api/_core/procurement/sto.handlers.ts` so STO creation now requires and persists `sending_cost_center_id` and `receiving_cost_center_id`, validates and persists PO-style per-line fields (`transfer_price` as Rate, `payment_term_id`, `freight_term`, `gst_terms`, `remarks`, `has_rebate`, `rebate_rate`, `rebate_rate_uom_basis`, `rebate_remarks`, `expected_delivery_date`) on both INTER_PLANT and CONSIGNMENT_DISTRIBUTION paths, enriches auto-created STO CSNs with payment-term and rebate metadata, and adds `getLastStoPaymentTermHandler` / `getLastUsedPaymentTermForStoPair()` for pair-based payment-term prefilling; updated `supabase/functions/api/_core/procurement/csn.handlers.ts` so the available-sub-CSN lookup now returns mother-PO-derived defaults such as Rate, Payment Term, Freight Term, GST Terms, expected delivery date, and rebate fields for prefilling consignment STO lines; registered the new `GET /api/procurement/stos/last-payment-term` route in both `supabase/functions/api/_routes/procurement.routes.ts` and `supabase/functions/api/_acl/route-acl-registry.ts`; added `getLastStoPaymentTerm()` in `frontend/src/pages/dashboard/procurement/procurementApi.js`; created `frontend/src/pages/dashboard/procurement/sto/StoCreateFormPage.jsx` as the shared PO-style STO form shell used by both `frontend/src/pages/dashboard/procurement/sto/STOCreatePage.jsx` and `frontend/src/pages/dashboard/procurement/sto/STOCreateOpeningPage.jsx`, with header-level company/cost-center fields, Material Type filter, PO-style line grid, per-line More drawer, pair-based payment-term prefilling, no storage-location fields, and preserved consignment sub-CSN picker behavior; and updated `frontend/src/pages/dashboard/procurement/sto/STODetailPage.jsx` so dispatch now surfaces an inline per-line LocationSelect form, saves missing sending/receiving locations through `updateSTO()`, and only then allows dispatch to proceed.
- Notes: Verified with `npm.cmd run build` in `frontend/` and an escalated `tsx` import check covering `supabase/functions/api/_core/procurement/sto.handlers.ts`, `supabase/functions/api/_core/procurement/csn.handlers.ts`, `supabase/functions/api/_routes/procurement.routes.ts`, and `supabase/functions/api/_acl/route-acl-registry.ts`; explicitly checked that the new last-payment-term endpoint was registered in both the route dispatcher and the ACL registry.

### 2026-06-25 18:31 IST
- Task: Closed the remaining STO vs PO parity gaps for approval audit history, amendment workflow, DRAFT editing, and resolved user display names.
- Changes: Updated `supabase/functions/api/_core/procurement/sto.handlers.ts` to add generic `*_by` display-name enrichment via `resolveUserDisplayNames`, include enriched approval/amendment logs in `hydrateSto`, enrich list-STO rows, add `insertStoApprovalLog`, log confirm/approve/reject transitions into `sto_approval_log`, add `amendSTOHandler` and `approveSTOAmendmentHandler` modeled on the PO flow, and keep the new amend / approve-amendment routes aligned with STO state transitions; registered the new STO amendment routes in both `supabase/functions/api/_routes/procurement.routes.ts` and `supabase/functions/api/_acl/route-acl-registry.ts`; added `amendSTO()` and `approveSTOAmendment()` in `frontend/src/pages/dashboard/procurement/procurementApi.js`; and rebuilt `frontend/src/pages/dashboard/procurement/sto/STODetailPage.jsx` to add a DRAFT-only edit modal, a CREATED-only amend modal, approval/amendment/audit cards, approval-amendment action wiring, and display-name fallbacks using the enriched `*_display` fields while preserving the dispatch-prep location capture flow added earlier.
- Notes: Verified with `npm.cmd run build` in `frontend/` and an escalated `tsx` import check covering `supabase/functions/api/_core/procurement/sto.handlers.ts`, `supabase/functions/api/_routes/procurement.routes.ts`, and `supabase/functions/api/_acl/route-acl-registry.ts`; explicitly rechecked that every new STO amend route was added to both the route dispatcher and the ACL registry.

### 2026-06-26 10:43 IST
- Task: Fixed the STO/PO parity follow-up navigation regression on procurement drill-through screens and resolved raw UUID master-data rendering on STO detail.
- Changes: Updated `frontend/src/pages/dashboard/procurement/po/POOrderGroupListPage.jsx` to open PO-order-group and STO detail screens through `openScreenWithContext(..., { refreshOnReturn: true })` before routing, updated `frontend/src/pages/dashboard/procurement/po/POOrderGroupDetailPage.jsx` to preserve screen-stack context when opening PO detail rows and to fall back cleanly to the approvals list via `popScreen()`/`openScreen()`, updated `frontend/src/pages/dashboard/procurement/po/POCreatePage.jsx`, `frontend/src/pages/dashboard/procurement/po/POCreateOpeningPage.jsx`, `frontend/src/pages/dashboard/procurement/sto/StoCreateFormPage.jsx`, and `frontend/src/pages/dashboard/procurement/qa/QADocumentPage.jsx` to pair procurement `navigate()` drill-through/fallback routes with the matching screen-stack call, and updated `frontend/src/pages/dashboard/procurement/sto/STODetailPage.jsx` to build sending-cost-center, receiving-cost-center, and payment-term label maps from its existing queries so header and line displays show readable labels instead of raw UUIDs.
- Notes: Verified with `eslint` on the touched frontend files (no new errors; only pre-existing hook-dependency warnings in create pages), `npm.cmd run build` in `frontend/`, and a tail/line-count structural check on every touched file to guard against stranded-code / premature-closing-brace regressions.

### 2026-06-27 11:47 IST
- Task: Started the CSN redesign foundation from the locked 9-part brief by landing the schema groundwork plus the first lifecycle-alignment changes at a clean verified boundary.
- Changes: Added `supabase/migrations/20260627110000_csn_redesign_foundation.sql` to introduce the first-pass CSN redesign schema pieces (`purchase_order.destination_port_id`, `purchase_order_line.knocked_off_qty`, new `consignment_note` courier/inactivation fields, the short CSN status-code check constraint, and the new `erp_procurement.csn_tracker_layout` / `erp_procurement.csn_field_history` tables with grants); updated `supabase/functions/api/_core/procurement/po.handlers.ts` so PO-driven CSN generation now skips `delivery_type = BULK`, treats `TANKER` like normal import/domestic classification, seeds `material_category_id` from the material's primary assignment, carries `destination_port_id` into new CSNs, writes short CSN status codes, and inactivates linked CSNs with distinct `CAN` / `KOF` reason tracking during PO cancel/knock-off; updated `supabase/functions/api/_core/procurement/sto.handlers.ts` so STO-created CSNs now use short status codes, cancellation inactivates linked CSNs (and clears `sto_id` for consignment-distribution rows), and `buildConsignmentStoFromSubCsns()` always sources `material_id` from the fetched sub-CSN rather than caller input; updated `supabase/functions/api/_core/procurement/csn.handlers.ts` so editable/read-only checks use the new codes, sub-CSN creation validates against and subtracts from the mother's remaining `dispatch_qty`, sub-CSN delete restores the mother's retained `dispatch_qty` and blocks STO-linked rows, tracker/detail enrichment now exposes `csn_display_number` plus PO-vs-STO reference substitution, port-to-plant transit lookup prefers `consignee_company_id` for allotted sub-CSNs, and all tracker alert/status filters now exclude/compare against the new codes; updated `supabase/functions/api/_core/procurement/gate_entry.handlers.ts`, `supabase/functions/api/_core/procurement/grn.handlers.ts`, and `supabase/functions/api/_core/procurement/planning.handlers.ts` to consume the short CSN codes (`ORD`, `TRN`, `GED`, `GRD`, `CAN`, `KOF`) consistently; and updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` plus `frontend/src/pages/dashboard/procurement/csn/CSNDetailPage.jsx` so the tracker/detail UI renders the new status badges/codes, shows sub-CSNs as `Sub-CSN-<number>` until STO-linked, and swaps the reference display from PO number to STO number when `sto_id` is present.
- Notes: Verified with `npx.cmd eslint` on the changed CSN frontend files, `npm.cmd run build` in `frontend/`, an escalated backend `tsx` import check covering `po.handlers.ts`, `sto.handlers.ts`, `csn.handlers.ts`, `gate_entry.handlers.ts`, `grn.handlers.ts`, and `planning.handlers.ts`, plus a tail/closing-brace structural integrity check on every touched file; no new routes were added in this milestone, so `procurement.routes.ts` / `_acl/route-acl-registry.ts` intentionally remained unchanged here.

### 2026-06-27 18:42 IST
- Task: Continued the CSN redesign brief with the remaining tracker-driven pieces: dispatch balance preview/confirm flow, expand-row edit/save UX, conditional highlighting, saved layouts, and field history wiring.
- Changes: Updated `supabase/functions/api/_core/procurement/csn.handlers.ts` to (1) enrich tracker/detail rows with material-group options, payment-term metadata, actual-payment-date calculation, created-by display labels, gate-entry/GRN labels, and lead-time snapshots; (2) switch CSN recalculation to the brief's ETD/ATD-driven domestic/import priority chain using PO-line `expected_delivery_date`; (3) widen the mutable/manual CSN field set and write one `erp_procurement.csn_field_history` row per changed field on both full-row saves and inline edits; (4) add dispatch-balance preview/confirm handlers that support first-split `Create CSN`, partial `Knock Off`, and multi-CSN reconciliation; and (5) add tracker-layout list/create/delete handlers plus field-history read handlers. Updated `supabase/functions/api/_routes/procurement.routes.ts` and `supabase/functions/api/_acl/route-acl-registry.ts` together to register every new CSN endpoint in both the dispatcher and the ACL gateway: `GET/POST /api/procurement/tracker/layouts`, `DELETE /api/procurement/tracker/layouts/:id`, `GET /api/procurement/csns/:id/history`, and `POST /api/procurement/csns/:id/dispatch-qty/(preview|confirm)`. Updated `frontend/src/pages/dashboard/procurement/procurementApi.js` with matching API helpers for layouts, history, and dispatch preview/confirm. Rebuilt `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` into a custom expandable tracker table with inline expand-row editing, per-field history popovers, dispatch split/reconciliation modals, conditional red-cell highlighting, day-diff calculated columns, actual-payment-date display, and named column-visibility layouts. Added `supabase/migrations/20260627173000_csn_add_boe_date.sql` because `BOE Date` is part of the locked brief field list but was still missing from the repo schema history.
- Notes: Verified with `npm.cmd run build` in `frontend/`, an escalated backend `tsx` import check covering `csn.handlers.ts`, `procurement.routes.ts`, and `_acl/route-acl-registry.ts`, plus an `eslint` pass on `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx`; explicitly rechecked that each new CSN route was added to both the route dispatcher and `route-acl-registry.ts`. The new `boe_date` migration file was created only, not applied.

### 2026-06-27 20:03 IST
- Task: Completed Part 10 of the locked CSN redesign brief by closing the remaining CSN Tracker column, expand-row, and density gaps.
- Changes: Updated `supabase/functions/api/_core/procurement/csn.handlers.ts` so tracker enrichment now returns `material_code`, `base_uom_code`, and `mother_csn_display_number` for sub-CSN reference display while keeping the existing Part 1-9 payload shape intact; updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` to add the remaining Part 10 grid coverage (`Material Code`, `Base UOM`, `Order UOM`, `PO Date`, `Has Rebate`, `Rebate Rate`, `Rebate Remarks`, `Calculated LC Opening Date`, `Baseline ETD/ETA`, and `Destination Port`), wire ATD through the save payload as the editable import/domestic driver field, add `Courier Date to CHA`, `Courier CHA Receive Date`, and read-only `Mother CSN` in the expand-row editor, show 100-row paging with tighter SAP-style spacing, and switch the top actions to compact tooltip-backed controls; updated `frontend/src/components/templates/ErpScreenScaffold.jsx` so action buttons can surface an explicit `title` tooltip for those compact tracker actions.
- Notes: Verified with a fresh `npm.cmd run build` in `frontend/`, a scoped escalated `eslint` run on `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` and `frontend/src/components/templates/ErpScreenScaffold.jsx`, and a structural-integrity tail/line-count check on both changed frontend files. A full project `npm.cmd run lint` still reports many unrelated pre-existing issues outside this task; no new tracker-specific lint errors remained after the scoped verification.

### 2026-06-27 21:34 IST
- Task: Corrected the CSN Tracker / CSN Detail follow-up gaps against the agreed tracker-first workflow after screenshot-based root-cause review.
- Changes: Updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` to make the locked working surface actually discoverable and single-surface: added a row-associated `Create Sub CSN` action beside the expand toggle, reorganized the expanded editor into explicit sections (`Allocation`, `Shipment Timeline`, `Documents`, `Logistics`, `Receiving`, `Courier And CHA`, `Remarks`) so BL/ETD/Invoice/BOE fields are visible in the right business grouping, added an expanded-row summary bar with `Open Detail`, and tightened the tracker header/filter layout so Layout/Delete/Search no longer crowd each other; also switched the tracker action strip to compact icon-style controls with tooltips. Updated `frontend/src/pages/dashboard/procurement/csn/CSNDetailPage.jsx` to stop owning duplicate split-dispatch workflow by removing the in-page `Add Sub CSN` / delete actions and replacing them with a tracker-first guidance note plus read-only Sub-CSN reference grid. Updated `frontend/src/pages/dashboard/procurement/po/PODetailPage.jsx` to build local payment-term and cost-center label maps from the queries already on the page so PO detail lines fall back to readable labels instead of raw UUIDs whenever backend display fields are absent.
- Notes: Root cause was not backend data loss; it was frontend workflow drift. The tracker had the Part 10 fields in code, but they were buried in one long ungrouped vertical form, while Sub-CSN ownership was still split between Tracker and Detail. Verified with scoped `eslint` on the three touched frontend files, `npm.cmd run build` in `frontend/`, and a structural tail/closing-brace integrity check on all touched files.

### 2026-06-27 22:18 IST
- Task: Implemented Part 11 of the locked CSN Tracker brief to bring the tracker onto the approved dense SAP-style working surface.
- Changes: Updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` to replace the oversized alerts/header area with a compact single-line tracker bar and icon-only tooltip actions, compress the filter and pagination strips, increase table row density, switch row interaction to single-click expand/collapse plus double-click detail navigation through `openScreenWithContext(..., { refreshOnReturn: true })`, add compact expand-row icon actions, add the new inline `Sub CSNs` section with create/delete wiring that reuses the existing sub-CSN endpoint rules (`ORD` and no `sto_id` only), and move Mother CSN into an always-visible bottom strip with a dash empty state for non-sub rows.
- Notes: Verified with `npm.cmd run build` in `frontend/`, a repo-wide `npm.cmd run lint` pass that still reports many unrelated pre-existing errors but no new `CSNTrackerPage.jsx` failures, and a structural tail/closing-brace check on `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` (`last_closing=1556`, `total=1557`).

### 2026-06-28 00:34 IST
- Task: Implemented CSN redesign brief Parts 12, 13, and 14 by closing the live Tracker bug round, adding balance/currency/rebate fixes, and auto-creating CSNs for `INTER_PLANT` STOs.
- Changes: Updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` to keep the tracker text-glyph-only (no icon-font classes), add a real `ALL` company filter, add `Balance Qty`, `PO Rate`, `Currency`, and `Delivery Type` columns, default allotted company visually to the owning company, gate import-only fields off for domestic rows, add editable rebate controls, keep expand-row actions visible without horizontal scrolling, wire keyboard shortcuts (`Esc`, `Ctrl+S`, `Ctrl+Shift+S`, `Ctrl+O`), and change dispatch-balance handling so any remainder always prompts `Create CSN` / `Knock Off` with manual reallocation as an explicit alternate action; updated `frontend/src/pages/dashboard/procurement/csn/CSNDetailPage.jsx` to remove the obsolete `BULK` branch now that `csn_type` is strictly `IMPORT`/`DOMESTIC`; updated `frontend/src/pages/dashboard/procurement/po/POCreatePage.jsx`, `frontend/src/pages/dashboard/procurement/po/POCreateOpeningPage.jsx`, and `frontend/src/pages/dashboard/procurement/sto/StoCreateFormPage.jsx` to add per-line INR/USD currency dropdowns while preserving their existing create flows; updated `supabase/functions/api/_core/procurement/csn.handlers.ts` so tracker enrichment now computes `balance_qty`, exposes PO/STO rate and currency, keeps `po_qty` immutable during dispatch split/reconcile flows, scopes company filtering across all ACL-allowed companies and both `company_id` / `consignee_company_id`, and resolves STO-origin vendor display names from companies when `vendor_id` stores the sending company; updated `supabase/functions/api/_core/procurement/po.handlers.ts` so new PO-driven CSNs inherit `delivery_type` and PO lines persist `currency_code`; and updated `supabase/functions/api/_core/procurement/sto.handlers.ts` so `INTER_PLANT` STO creation now auto-creates one CSN per STO line with the locked origin mapping (`po_id`/`po_line_id` null, `sto_id` set, `company_id` = sender, `consignee_company_id` = receiver, `vendor_id` = sender company id, `dispatch_qty` = 0, `delivery_type` copied, `csn_type` derived from sender GST presence), while STO lines persist the new currency field for both inter-plant and consignment-distribution paths.
- Notes: Verified with scoped `eslint` on the touched frontend files (clean), `npm.cmd run build` in `frontend/` (clean), a structural tail/closing-brace integrity check on every touched file, and `deno check supabase/functions/api/_core/procurement/csn.handlers.ts` reduced to only the repo's pre-existing shared typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`. No new routes were added in this round, so `procurement.routes.ts` and `_acl/route-acl-registry.ts` correctly remained unchanged.

### 2026-06-28 01:04 IST
- Task: Implemented CSN redesign brief Part 15 by correcting the STO-linked CSN company/vendor role mapping for both `CONSIGNMENT_DISTRIBUTION` linking and `INTER_PLANT` auto-created CSNs.
- Changes: Updated `supabase/functions/api/_core/procurement/sto.handlers.ts` so `buildConsignmentStoFromSubCsns()` now flips a linked sub-CSN to `company_id = receiving_company_id`, `vendor_id = sending_company_id`, and `consignee_company_id = null` at the moment `sto_id` is set, without touching CSN `status` or `mother_csn_id`; and updated `buildCsnForInterPlantStoLine()` so newly auto-created `INTER_PLANT` CSNs are born with `company_id = sto.receiving_company_id`, `vendor_id = sto.sending_company_id`, and `consignee_company_id = null`, matching the already-corrected dev DB example `CSN-000005`.
- Notes: Verified with `npm.cmd run build` in `frontend/`, a backend `tsx` import check for `supabase/functions/api/_core/procurement/sto.handlers.ts` (`sto-handlers-import-ok`), a structural tail/closing-brace check on `sto.handlers.ts`, and `deno check supabase/functions/api/_core/procurement/sto.handlers.ts`, which still reports only the repo's pre-existing shared typing noise (`_pipeline/context.ts`, `_shared/canonical_access.ts`, `_shared/serviceRoleClient.ts`) plus the longstanding `DbQueryBuilder.or(...)` typing mismatch already present in this file outside the Part 15 edits.

### 2026-06-29 00:18 IST
- Task: Implemented CSN redesign brief Part 17 by adding the missing CHA field to the CSN Tracker using the same linked-dropdown plus freetext fallback pattern already used for transporters.
- Changes: Updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` to reuse the existing `listCHAs({ is_active: true, limit: 500 })` pattern, add a read-only `CHA` grid column, add `CHA` and `CHA Name (if not listed)` fields in the Logistics section, bind them to `cha_id` and `cha_name_freetext`, and disable freetext when a master CHA is selected; updated `supabase/functions/api/_core/procurement/csn.handlers.ts` to whitelist `cha_id` and `cha_name_freetext` for tracker saves, batch-load `cha_master` rows during tracker enrichment, and return a resolved `cha_name` field that falls back to freetext when no linked master row exists.
- Notes: Verified with scoped `eslint` on `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx`, `npm.cmd run build` in `frontend/`, structural tail/closing-brace checks on the touched frontend/backend files, and `deno check supabase/functions/api/_core/procurement/csn.handlers.ts`, which still reports only the repo's pre-existing shared typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`.

### 2026-06-29 00:29 IST
- Task: Implemented CSN redesign brief Part 18 by replacing the Tracker's plain Transporter and CHA dropdowns with the existing searchable combobox component.
- Changes: Updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` to reuse `frontend/src/components/forms/ErpComboboxField.jsx` for both `transporter_id` and `cha_id`, replacing the plain `<select>` elements with searchable, keyboard-navigable comboboxes. The option labels now use the locked `Name — Address` format for both transporters and CHAs, with `"No address on file"` as the fallback location text, so the user never sees raw UUIDs in the open or collapsed field state.
- Notes: Verified with scoped `eslint` on `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx`, `npm.cmd run build` in `frontend/`, and a structural tail/closing-brace check on the touched file. The grid's read-only `Transporter` / `CHA` columns remain unchanged and continue to show the resolved names only.

### 2026-06-29 00:42 IST
- Task: Implemented CSN redesign brief Part 19 by scoping the Tracker's CHA dropdown to the expanded row's own company while preserving unfiltered admin behavior elsewhere.
- Changes: Updated `supabase/functions/api/_core/procurement/l2_masters.handlers.ts` so `listCHAsHandler` now accepts an optional `company_id` query param; when present it reads active mappings from `erp_master.cha_company_map`, derives the allowed `cha_id` list, and filters `cha_master` with `.in("id", allowedChaIds)`, while leaving the existing unfiltered behavior unchanged when `company_id` is absent. Updated `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx` so the `chasQuery` is keyed by the expanded row's own `company_id` and sends that `company_id` to `listCHAs(...)`, ensuring sub-CSN / STO-flipped rows fetch the correct company-scoped CHA list independent of the Tracker's top-level filter.
- Notes: Verified with scoped `eslint` on `frontend/src/pages/dashboard/procurement/csn/CSNTrackerPage.jsx`, `npm.cmd run build` in `frontend/`, structural tail/closing-brace checks on both touched files, and `deno check supabase/functions/api/_core/procurement/l2_masters.handlers.ts`, which still reports only the repo's pre-existing shared typing noise plus existing `DbQueryBuilder` typing mismatches elsewhere in that file, not from this Part 19 change. A direct `tsx` import retry hit the same intermittent Windows `EPERM lstat C:\\Users\\cpalm` environment issue seen in earlier rounds.

### 2026-07-08 00:00 IST
- Task: Completed an audit-only scan for per-item sequential / fan-out DB and API loops across `supabase/functions` and `frontend/src`, and wrote the findings to `docs/Codex-Audit-Sequential-Loops.md`.

### 2026-07-08 23:19 IST
- Task: Fixed the independent sequential-loop hotspots called out for `supabase/functions/api/_core/procurement/po.handlers.ts` under Claude rule 8B.
- Changes: Batched the PO->CSN creation prechecks by replacing per-line existing-CSN reads with one `.in("po_line_id", ...)` query and replacing per-line material-category reads with one `.in("material_id", ...)` lookup, then kept doc-number generation and CSN inserts per line but ran those independent writes through `Promise.all`; batched PO-line preparation lookups by preloading all referenced cost centers in one query and all approved ASL/VMI rows plus their UOM rows in one batched pass before the loop; batched per-material PO creation prerequisites by building all prepared lines in one call and loading all payment terms in one `.in("id", ...)` query, then parallelized the per-material PO header number-generation + header insert + line insert work with `Promise.all` while preserving result order.
- Notes: Safe because each targeted iteration is independent per the audit: one PO line's CSN prechecks do not depend on another line, PO line validation reads only master data, and each material in the create flow still creates its own standalone PO + line pair; single-line POs keep the same behavior and validation path, only without unnecessary repeated lookups when multiple materials/lines are present. `deno check supabase/functions/api/_core/procurement/po.handlers.ts` was rerun after the refactor and still reports pre-existing type errors in older untouched sections of `po.handlers.ts` plus shared files (`_pipeline/context.ts`, `_shared/canonical_access.ts`, `_shared/serviceRoleClient.ts`), but no new checker errors remained in the newly edited batching code.

### 2026-07-09 00:00 IST
- Task: Fixed the HR independent sequential-loop hotspots called out in `docs/Codex-Audit-Sequential-Loops.md` for leave and out-work handlers under Claude rule 8B.
- Changes: Added a shared batched `loadActiveCompanyWorkContextsByCompany()` helper in `supabase/functions/api/_shared/workflow_scope.ts` so `supabase/functions/api/_core/hr/leave.handlers.ts` and `supabase/functions/api/_core/hr/out_work.handlers.ts` now build their work-context maps from one `.in("company_id", ...)` read across all distinct companies instead of one query per company; changed `expandLeaveToDateRecords()` and `expandOutWorkToDateRecords()` to issue the per-date RPC upserts with `Promise.all` instead of awaiting one date at a time.
- Notes: Safe because the work-context lookup is pure master-data hydration grouped back in memory by `company_id`, and both day-record RPCs were checked against their SQL definitions in `supabase/migrations/20260428080000_80_security_fix_definer_functions.sql`: each RPC only upserts one `(company_id, employee_auth_user_id, record_date)` row with holiday/week-off guards and does not decrement or carry a running leave/out-work balance that would make prior-date order matter. No existing documented `DEPENDENT` loops were changed.
## 2026-07-09 - Procurement Sequential Loop Batching

- Batched independent procurement loops in `l2_masters.handlers.ts`, `gate_entry.handlers.ts`, `invoice_verification.handlers.ts`, `sales_order.handlers.ts`, and `sto.handlers.ts` by replacing per-row validation reads with `.in()` lookups and replacing independent per-row updates/inserts with `Promise.all`, while preserving existing business logic and header-total recomputes.
- Safe batching details: CHA-port validation now resolves all requested ports in one set-based read before the existing bulk upsert; inbound gate-exit net-weight writeback now parallelizes independent line updates; gate-entry prune now restores CSNs in grouped bulk updates by shared target status; IV match recompute now keeps the running header totals in-process while parallelizing only the independent line writes; sales-invoice creation now loads all source SO lines once; inter-plant STO CSN creation now mirrors the PO CSN helper by batching existence and payment-term lookups before parallel doc-number generation plus insert; STO editable line updates now run in parallel because each patch targets a separate line row.
- Added `DEPENDENT:` guard comments with zero logic changes above the audited procurement loops in `grn.handlers.ts`, `inward_qa.handlers.ts`, `opening_stock.handlers.ts`, `physical_inventory.handlers.ts`, `rtv.handlers.ts`, `sales_order.handlers.ts`, and the three dependent STO loops in `sto.handlers.ts` so future optimization attempts keep the required ordering constraints intact.

## 2026-07-09 - Frontend Independent Loop Batching

- Updated `frontend/src/admin/sa/screens/SACapabilityGovernance.jsx` to parallelize the independent capability-matrix save writes and bulk pack-content removals with `Promise.all`, while leaving the already-parallel pack-count and work-context-capability loaders unchanged.
- Updated `frontend/src/admin/sa/screens/SAMaterialMaster.jsx` and `frontend/src/admin/sa/screens/SAVendorMaster.jsx` so bulk activate/deactivate actions now issue their per-record status changes in parallel with `Promise.all` instead of awaiting one id at a time.
- Rechecked `frontend/src/pages/dashboard/procurement/accounts/IVCreatePage.jsx`, `frontend/src/pages/dashboard/procurement/accounts/LandedCostListPage.jsx`, `frontend/src/pages/dashboard/procurement/rtv/RTVListPage.jsx`, and `frontend/src/pages/dashboard/procurement/procurementApi.js`: there is still no bulk GRN-by-ids helper in the API layer, and each audited page was already using ordered `Promise.all` over the original id list, so no code change was needed there.
- Safe because each touched iteration is independent: one capability resource-row save/disable does not depend on another row, one selected pack rule removal does not depend on another removal, and each material/vendor status toggle targets a separate record with no shared running state; where result ordering matters, the code still maps over the original array and relies on `Promise.all` preserving input order.

## 2026-07-09 - OM Independent Loop Batching

- Updated `supabase/functions/api/_core/om/material.handlers.ts` so bulk material deletes now preserve per-id success/error reporting while running the independent deletes concurrently with `Promise.all`, and CSV company-mapping import now does all row parsing/lookup validation first, then parallelizes the independent `material_company_ext` upserts while returning results in the original CSV row order.
- Updated `supabase/functions/api/_core/om/vendor.handlers.ts` so bulk vendor deletes keep the existing per-id result granularity but no longer await each delete serially.
- Updated `supabase/functions/api/_core/om/vendor_material_info.handlers.ts` so VMI create/update UOM validation now loads all distinct requested UOM codes in one `.in("code", ...)` query and checks membership in memory instead of querying one code at a time.
- Safe because every touched iteration is independent: one delete targets one master row and reports its own outcome, one CSV mapping row upserts one `(material_id, company_id)` pair without depending on another row's completion, and each requested UOM code validation is a pure existence check against shared master data.

### 2026-07-11 11:06 IST
- Task: Fixed the Process PO create frontend 400 blocker on PR09.
- Changes: Updated `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` so the Process PO form state now includes `segment_code`, added a required Segment dropdown after Production Type with the locked options `ADMIX`, `HPS`, `IWC`, `POWDER`, and `INT`, changed the create payload to send `po_type` instead of `prod_type` plus the new `segment_code`, and completed the file header with `Phase` and `Authority`.
- Notes: Scope stayed frontend-only and single-file; the Packing PO path was left functionally untouched. `npm.cmd run lint -- src/pages/dashboard/production/ProductionPOCreatePage.jsx` still executed the repo-wide ESLint script and failed on many pre-existing unrelated errors outside this file, so verification here relied on the targeted grep checks plus file read-through for the touched screen.

### 2026-07-11 12:40 IST
- Task: Implemented Gate-27.5 QA Queue field/display correctness fix for PR16.
- Changes: Updated `supabase/functions/api/_core/production/process_order.handlers.ts` so `listProcessOrdersHandler` now batch-resolves `stroke_number` from `erp_production.stroke_master` and `created_by_display` via the shared `resolveUserDisplayNames()` helper, then returns both fields additively alongside the existing material resolution. Updated `frontend/src/pages/dashboard/production/QAQueuePage.jsx` to read the correct backend fields (`po_type`, `planned_qty`, `stroke_number`, `created_by_display`), remove raw-UUID fallbacks for prodshade/stroke/created-by/material line display, and show `--` in the interim Dosage% column instead of a fake numeric fallback.
- Notes: Scope stayed exactly on brief: no route, ACL, migration, status-machine, `fetchOrderLines`, or `getProcessOrderHandler` changes. Verification used targeted grep/read-through: the old `prod_type`, `planned_qty_kg`, and raw `slice(0, 8)` fallbacks for QA queue display were removed, and the list handler now attaches both required display fields through batched lookups.

### 2026-07-11 13:05 IST
- Task: Implemented Gate-27.4 reservation-at-Standard, prune-at-STANDARD, and availability netting for Process POs.
- Changes: Added `supabase/migrations/20260711100000_gate27_reservation_document_and_prune.sql` to create `erp_production.reservation_document`, `reservation_number_seq`, related indexes/grants, and the `process_order` prune columns plus `CANCELLED` status constraint. Updated `supabase/functions/api/_core/production/process_order.handlers.ts` so `checkStockAvailability()` now subtracts open/partial reservations at the interim material+company level, `createProcessOrderHandler()` collects inserted lines and batch-creates OPEN reservation rows after Standard save, and added `pruneProcessOrderHandler()` to cancel a STANDARD Process PO plus all linked reservations with reason/audit metadata. Wired the new endpoint through `supabase/functions/api/_routes/production.routes.ts` and registered `POST /api/production/process-orders/:id/prune` in `supabase/functions/api/_acl/route-acl-registry.ts`.
- Notes: Scope stayed backend-only per brief: no frontend, no verify/finalize/start-batch/reverse redesign, no reservation issue-at-Verify logic. Sanity verification used targeted grep/read-through for the new migration, reservation query/insert path, route dispatch, and ACL registration. A direct local `tsx` import check failed before reaching these files because `supabase/functions/api/_shared/serviceRoleClient.ts` throws `SERVICE_ROLE_NOT_CONFIGURED` when the required env vars are absent in this shell, so runtime import validation remains for Claude's environment-backed pass.

### 2026-07-11 15:09 IST
- Task: Implemented Gate-27.6 Process PO full-chain migration, backend lifecycle changes, and frontend PR09/PR10/PR11/PR12/PR15 plus minimal INT completion surface in brief order.
- Changes: Added `supabase/migrations/20260711120000_gate27_processpo_full_chain.sql` for the new `process_order` columns (`machine_id`, `has_unapproved_deviation`, `qi_release_stock_ledger_id`), the new `process_order_line` final/verify columns, and the new `erp_production.process_order_line_reco` table plus indexes/grant. Rebuilt `supabase/functions/api/_core/production/process_order.handlers.ts` to add machine validation on create, MTEST immediate verify-on-create flow, QA reject direct cancel+prune behavior, MTS start-batch status branching, `po_type_in` support on list, new `editProcessOrderHandler`, new `completeIntProcessOrderHandler`, Final approved/AP-approved/variance/substitution handling, Verify QI-hold + P321 auto-release + reservation issue + reco commit handling, and verified reverse flow updates including P322 and reco voiding. Wired the new `/edit` and `/complete-int` routes in `supabase/functions/api/_routes/production.routes.ts` and `supabase/functions/api/_acl/route-acl-registry.ts`. Updated `frontend/src/pages/dashboard/production/prodApi.js` for the new endpoints/body shape, rebuilt `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` around comboboxes for company/prodshade/stroke/machine, rebuilt `frontend/src/pages/dashboard/production/ProductionPOEditPage.jsx` around the new PR10 endpoint, rebuilt `frontend/src/pages/dashboard/production/ProductionPOFinalPage.jsx` and `frontend/src/pages/dashboard/production/ProductionPOVerifyPage.jsx` to the locked input/output table shape, updated `frontend/src/pages/dashboard/production/QAQueuePage.jsx` to send `po_type_in=MTO,HPS`, rebuilt `frontend/src/pages/dashboard/production/ReversalPage.jsx` to enforce mandatory reason before confirm and show returned ledger entries, and added the lightweight INT `Complete` modal action to `frontend/src/pages/dashboard/production/OrderListPage.jsx`.
- Notes: Touched files were `supabase/migrations/20260711120000_gate27_processpo_full_chain.sql`, `supabase/functions/api/_core/production/process_order.handlers.ts`, `supabase/functions/api/_routes/production.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/prodApi.js`, `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOEditPage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOFinalPage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOVerifyPage.jsx`, `frontend/src/pages/dashboard/production/QAQueuePage.jsx`, `frontend/src/pages/dashboard/production/ReversalPage.jsx`, `frontend/src/pages/dashboard/production/OrderListPage.jsx`, and `docs/Codex-Log.md`. Flagged ambiguities instead of silently inventing them: the brief says the existing MTEST create UI already collects manual lines, but this repo's `ProductionPOCreatePage.jsx` did not contain that UI, so I did not invent a new manual-line builder in this round; the brief's backend section keeps the persisted reverse status update as `REVERSED` while frontend section 3.5 asks the page to show resulting status `STANDARD` or `CANCELLED`, so I left the backend/status model unchanged and surfaced the returned response status plus ledger entries; and the brief's verified reverse rewrite assumes the normal QI-hold/P321 path, while MTEST is a documented no-QI-hold exception, so the reverse handler conditionally falls back to the non-P321 reversal path when `qi_release_stock_ledger_id` is absent instead of guessing a new persisted status or movement sequence.

### 2026-07-11 18:50 IST
- Task: Implemented Gate-27.14 storage-location rearchitecture for Process POs in brief order: additive migration stub, backend location-authority/netting changes, then frontend PR09/PR11/PR12 wiring.
- Changes: Added `supabase/migrations/20260711140000_gate27_location_aware_stock_check.sql` as the required additive no-op migration. Updated `supabase/functions/api/_core/production/process_order.handlers.ts` so stroke-line prepopulation now selects `default_storage_location_id`, applies optional `line_location_overrides`, writes `issue_sloc_id` from stroke-line defaults instead of segment config, makes reservation storage locations follow `issue_sloc_id`, simplifies `getIssueStorageLocationId()` to the line field only, makes stock availability and reservation netting composite-keyed by `material_id + storage_location_id`, batches INT output-location crediting by stroke default location, adds the new `availabilityPreviewProcessOrderHandler`, supports `storage_location_id` overrides during Final/Verify line saves, removes segment-config reads from INT-complete / Verify / VERIFIED reverse paths, and changes MTEST to require request-supplied output and line storage locations instead of segment config. Wired the new read-only route `GET /api/production/process-orders/availability-preview` in `supabase/functions/api/_routes/production.routes.ts` and registered the matching ACL in `supabase/functions/api/_acl/route-acl-registry.ts` with the same `PROD_ORDER_LIST` + `VIEW` access as the nearest sibling read-only Process PO route. Updated `frontend/src/pages/dashboard/production/prodApi.js` with the new availability-preview API helper, `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` with the non-MTEST RM location grid, per-row storage-location comboboxes, rose highlighting, preview query, and `line_location_overrides` payload support, and updated `frontend/src/pages/dashboard/production/ProductionPOFinalPage.jsx` plus `frontend/src/pages/dashboard/production/ProductionPOVerifyPage.jsx` so the SLoc cell is now an `ErpComboboxField`, save payloads include `storage_location_id`, and availability-preview-driven rose highlighting tracks the row's current location.
- Notes: Touched files were `supabase/migrations/20260711140000_gate27_location_aware_stock_check.sql`, `supabase/functions/api/_core/production/process_order.handlers.ts`, `supabase/functions/api/_routes/production.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/prodApi.js`, `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOFinalPage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOVerifyPage.jsx`, and `docs/Codex-Log.md`. Flagged ambiguity instead of guessing: the brief says MTEST manual RM lines are entered in an existing `ProductionPOCreatePage.jsx` MTEST mode, but this repo still does not contain a manual-line MTEST editor, so I implemented the locked backend MTEST contract and logged the missing frontend surface rather than inventing a new MTEST line-builder UI. Verification notes: `deno check supabase/functions/api/_core/production/process_order.handlers.ts` and `deno check supabase/functions/api/_routes/production.routes.ts` still surface only pre-existing shared/re-export typing noise outside these edits (`_pipeline/context.ts`, `_shared/canonical_access.ts`, `_shared/serviceRoleClient.ts`, plus existing production files pulled in by route imports); targeted grep confirmed zero remaining `getSegmentLocConfig` call sites in `process_order.handlers.ts`, both stock-ledger and reservation-netting queries now filter on `material_id` and `storage_location_id`, and the touched frontend files were checked for raw-UUID SLoc fallbacks and mojibake.

### 2026-07-11 19:16 IST
- Task: Implemented Gate-27.15 exactly as locked: PR16 QA Queue rebuild plus the real PR17 Batch Number Release backend/frontend, while leaving `BatchReleasePage.jsx` untouched.
- Changes: Added `supabase/migrations/20260711190000_gate27_batch_number_instance.sql` because the repo had only `erp_production.batch_number_series` counters and no instance/lifecycle table for VOIDED/RELEASED rows. The new table columns are `id`, `company_id`, `po_type`, `prodshade_material_id`, `batch_number`, `status`, `source_process_order_id`, `voided_at`, `released_by`, `released_at`, `release_reason`, `created_at`, `last_updated_at`, and `last_updated_by`, plus a unique `(company_id, batch_number)` constraint and supporting indexes. Updated `supabase/functions/api/_core/production/batch_series.handlers.ts` with the new batch-number list/release handlers and reusable instance helpers; updated `supabase/functions/api/_core/production/process_order.handlers.ts` so `listProcessOrdersHandler` now batch-resolves machine display, `startBatchHandler` checks for RELEASED numbers by Company + PO Type before auto-generation and can activate a selected released number, MTEST/start-batch assignments create ACTIVE instance rows, and reverse marks batch-number instances VOIDED. Wired the new `GET /api/production/batch-numbers` route and `POST /api/production/batch-numbers/:id/release` route in `supabase/functions/api/_routes/production.routes.ts` and ACL-registered them in `supabase/functions/api/_acl/route-acl-registry.ts` using `PROD_BATCH_RELEASE` `VIEW`/`WRITE`. Updated `frontend/src/pages/dashboard/production/prodApi.js` with `listBatchNumbers()`, `releaseBatchNumber()`, and optional-body `startBatch()`. Rebuilt `frontend/src/pages/dashboard/production/QAQueuePage.jsx` into the locked inline expandable queue, added the Start Batch released-number picker, and created the new `frontend/src/pages/dashboard/production/BatchNumberReleasePage.jsx` for PR17. Updated `frontend/src/router/AppRouter.jsx` so `/dashboard/production/batch-release` now points to the new PR17 page without editing the legacy `frontend/src/pages/dashboard/production/BatchReleasePage.jsx` file itself.
- Notes: Touched files were `supabase/migrations/20260711190000_gate27_batch_number_instance.sql`, `supabase/functions/api/_core/production/batch_series.handlers.ts`, `supabase/functions/api/_core/production/process_order.handlers.ts`, `supabase/functions/api/_routes/production.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/prodApi.js`, `frontend/src/pages/dashboard/production/QAQueuePage.jsx`, `frontend/src/pages/dashboard/production/BatchNumberReleasePage.jsx`, `frontend/src/router/AppRouter.jsx`, and `docs/Codex-Log.md`. `frontend/src/pages/dashboard/production/BatchReleasePage.jsx` was intentionally left untouched per brief §0. For PR16 sorting I took the brief-approved frontend path: kept the backend `created_at DESC` order and did the pending-first (`STANDARD` first) stable sort in `QAQueuePage.jsx`. For capability-gated actions I took the brief’s fallback simplification because this production area does not already expose a clearer capability-check rendering pattern: any user who can reach PR16 sees the `STANDARD` Approve/Reject actions and `QA_APPROVED` Start Batch action. Verification notes: the new batch-number routes are ACL-registered; `QAQueuePage.jsx` no longer uses the old separate-drawer pattern and now expands inline; `deno check` on `batch_series.handlers.ts`, `process_order.handlers.ts`, and `production.routes.ts` still reports only the repo’s pre-existing shared/imported typing noise outside these edits; and targeted grep confirmed the new PR17 page file/routing was added without editing the old `BatchReleasePage.jsx`.
### 2026-07-11 19:49 IST
- Task: Implemented `CODEX-GATE27.16-SFG-RESULT-RECORDING-TASK-BRIEF.md` in brief order: additive migration, cloned production backend handlers/routes/ACL, then the cloned production frontend page.
- Changes: Added `supabase/migrations/20260711203000_gate27_sfg_result_recording.sql` as the SFG-side clone of the existing Inward QA table set, mirroring `erp_procurement.inward_qa_document` / `erp_procurement.inward_qa_test_line` / `erp_procurement.inward_qa_decision_line` into `erp_production.sfg_qa_document` / `erp_production.sfg_qa_test_line` / `erp_production.sfg_qa_decision_line`, including the `test_method_id` / `lsl` / `usl` / `storage_location_id` redesign columns, matching grant pattern, and adding the required `erp_procurement.document_number_series` row for `SFG_QA`. Added `supabase/functions/api/_core/production/sfg_qa.handlers.ts` as the production clone of `supabase/functions/api/_core/procurement/inward_qa.handlers.ts`, retargeted from GRNs to VERIFIED Process POs filtered to `po_type IN ('MTO','HPS','MTS')`, with list/get/add-test-line/update-test-line/record-decision flow and no stock-posting call. Wired the new production endpoints through `supabase/functions/api/_routes/production.routes.ts` and `supabase/functions/api/_acl/route-acl-registry.ts` on the existing `PROD_QA_QUEUE` capability family. Updated `frontend/src/pages/dashboard/production/prodApi.js` with the new SFG QA endpoint wrappers. Added `frontend/src/pages/dashboard/production/SfgResultRecordingPage.jsx` as the production clone of `frontend/src/pages/dashboard/procurement/qa/QAQueuePage.jsx`, keeping the same list/detail/test-line-entry/decision-recording pattern but retargeting the list to VERIFIED Process POs and the detail to SFG QA documents. Updated `frontend/src/router/AppRouter.jsx` and `frontend/src/navigation/screens/projects/operationModule/operationScreens.js` to register the new production page route/screen.
- Notes: Touched files were `supabase/migrations/20260711203000_gate27_sfg_result_recording.sql`, `supabase/functions/api/_core/production/sfg_qa.handlers.ts`, `supabase/functions/api/_routes/production.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/prodApi.js`, `frontend/src/pages/dashboard/production/SfgResultRecordingPage.jsx`, `frontend/src/router/AppRouter.jsx`, `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, and `docs/Codex-Log.md`. Clone counterparts: new backend file `sfg_qa.handlers.ts` cloned from `supabase/functions/api/_core/procurement/inward_qa.handlers.ts`; new frontend file `SfgResultRecordingPage.jsx` cloned from `frontend/src/pages/dashboard/procurement/qa/QAQueuePage.jsx`; new migration cloned from the current Inward QA table shape defined across `erp_procurement.inward_qa_document`, `erp_procurement.inward_qa_test_line`, and `erp_procurement.inward_qa_decision_line` plus their redesign/index migrations. Ambiguities flagged instead of silently inventing them: the brief does not explicitly say whether `sfg_qa_document` rows should be created during Gate-27.6 Verify or lazily when this feature first lists eligible VERIFIED Process POs, so I kept Gate-27.6 untouched and had the new queue handler lazily ensure one document per eligible Process PO; the brief says to mirror `inward_qa_document` exactly while substituting Process-PO linkage, but does not explicitly lock whether GRN-only context columns such as `grn_line_id`, `po_id`, `vendor_id`, and `batch_lot_number` should survive in the SFG form, so the migration kept the direct production equivalents only (`process_order_id`, `batch_number`) instead of inventing meaningless GRN-era carryovers; and because the brief forbids any second stock posting while still asking for an exact decision-line shape, the new decision handler leaves `stock_document_id` / `stock_ledger_id` null and records `posting_status = 'POSTED'` as a documentary save-state, since no alternate non-posting status vocabulary was locked. Verification notes: `rg` confirmed no `postStockMovement` call exists anywhere in `supabase/functions/api/_core/production/sfg_qa.handlers.ts`; the new migration contains the six grant lines mirroring the Inward QA pattern; the eligible list is hard-filtered to `const ELIGIBLE_PO_TYPES = ["MTO", "HPS", "MTS"]` with `.in("po_type", ELIGIBLE_PO_TYPES)`, so INT/MTEST never enter this feature; `npm.cmd run build` in `frontend/` passed after the new page wiring; `deno check supabase/functions/api/_core/production/sfg_qa.handlers.ts`, `deno check supabase/functions/api/_routes/production.routes.ts`, and `deno check supabase/functions/api/_acl/route-acl-registry.ts` were run, and after fixing the fresh handler-local issues the remaining checker output fell back to the repo's pre-existing shared/imported typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, `_shared/serviceRoleClient.ts`, and other older production files already pulled in by route imports, not from new stock-posting logic in this feature.

### 2026-07-11 20:09 IST
- Task: Implemented `CODEX-GATE27.17-SFG-QA-CONCRETE-TRIAL-TASK-BRIEF.md` exactly as locked: one additive migration plus the single frontend follow-up in `SfgResultRecordingPage.jsx`.
- Changes: Added `supabase/migrations/20260711220000_gate27_sfg_qa_concrete_trial.sql` to widen `erp_master.qa_test_method.test_group` from `MCT/OTHR` to `MCT/OTHR/CT`, using the locked `qa_test_method_test_group_check` constraint name in the drop/re-add sequence. Updated `frontend/src/pages/dashboard/production/SfgResultRecordingPage.jsx` only to add `ctConfigs`, add the matching `ctMethodsQuery` + `ctMethods`, render a third `Concrete Trial (optional)` method group column beside MCT/OTHR, and map `CT` to `sfg_qa_test_line.test_type = "OTHER"` in the same way as OTHR.
- Notes: Touched files were `supabase/migrations/20260711220000_gate27_sfg_qa_concrete_trial.sql`, `frontend/src/pages/dashboard/production/SfgResultRecordingPage.jsx`, and `docs/Codex-Log.md`. The MCT-only gating block (`failedMctMethods`, `anyMctFail`, `allMctFilled`, `decisionSubmitDisabled`, and the decision submit guards/messages) was left untouched; verification by targeted search confirmed CT was added only to the new query/render/mapping spots and not to the mandatory-check logic. `frontend/src/pages/dashboard/procurement/qa/QAQueuePage.jsx` was not edited; a targeted search for `CT` / `Concrete Trial` / CT render wiring there returned zero hits. `npm.cmd run build` in `frontend/` passed after the change. Constraint-name note: this workspace did not provide live DB query access for the brief's `pg_constraint` verification step, so I used the existing canonical/generated name already reflected by the table-definition naming pattern and the brief's own locked SQL snippet: `qa_test_method_test_group_check`, and I am recording that limitation explicitly here instead of pretending a DB query was run.

### 2026-07-12 11:42 IST
- Task: Implemented the unambiguous engine-level portion of `CODEX-GATE27.19-BATCH-NUMBER-PERSISTENCE-TASK-BRIEF.md` after reading Section 83.7 (`Batch Number Persistence Mechanism`) in full: additive migration for `stock_ledger.batch_number` + `packing_order.batch_number`, backward-compatible `post_stock_movement()` overload extension, and the locked Process PO verify/CORS batch-number call-site updates.
- Changes: Added `supabase/migrations/20260712013000_gate27_batch_number_persistence.sql` to (1) hard-guard `erp_inventory.stock_ledger.batch_id` drop behind runtime checks for FK absence and zero non-null rows, (2) replace `stock_ledger.batch_id` with `stock_ledger.batch_number TEXT NULL`, (3) add `erp_production.packing_order.batch_number TEXT NULL`, and (4) drop/recreate both `erp_inventory.post_stock_movement()` overloads with one appended optional parameter `p_batch_number TEXT DEFAULT NULL`, writing that value into `stock_ledger.batch_number` while keeping all existing parameter positions/defaults intact. Updated `supabase/functions/api/_core/production/process_order.handlers.ts` so the local `postStockMovement(...)` RPC wrapper now forwards the optional `p_batch_number`, `verifyProcessOrderHandler()` passes `process_order.batch_number` only on the SFG receipt `P101`, and `reverseProcessOrderHandler()` passes the same batch on the verified CORS reversal postings `P262`, `P322`, and `P102` while leaving RM-issue `P261`, MTEST, INT, MTS, Opening Stock, PID, and all other call sites untouched. Verification: `Get-ChildItem ... | Select-String post_stock_movement` confirmed the broader codebase still calls the same RPC name with no required signature churn; `deno check supabase/functions/api/_core/production/process_order.handlers.ts` and `deno check supabase/functions/api/_core/production/packing_order.handlers.ts` now fall back only to the repo's already-documented shared typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`, with no new handler-local type errors from this change.
- Notes: Touched files were `supabase/migrations/20260712013000_gate27_batch_number_persistence.sql`, `supabase/functions/api/_core/production/process_order.handlers.ts`, and `docs/Codex-Log.md`. Important limitation recorded instead of guessed through: this environment did not provide live Dev DB query access, so I could not independently query current Dev rows/FKs before editing; instead the new migration itself re-verifies `stock_ledger.batch_id` at runtime via `information_schema` FK inspection plus a live `COUNT(*) WHERE batch_id IS NOT NULL` hard-stop before any drop occurs. Second ambiguity/blocker: the current repo's actual `supabase/functions/api/_core/production/packing_order.handlers.ts` Final path posts only PM `P261`, and its reversal posts only PM `P262`; there is no existing Packing Final SFG-issue `P261` plus FG-receipt `P101` flow, nor Packing reversal `P102`, to attach `p_batch_number` to. Because the brief explicitly forbids inventing extra design and says to stop on ambiguous packing-final timing/structure rather than guessing, I did not fabricate new Packing PO stock-movement behavior or set `packing_order.batch_number` at a made-up moment.

### 2026-07-12 01:05 IST
- Task: Implemented `CODEX-GATE27.18-PR09-STANDARD-CREATE-REBUILD-TASK-BRIEF.md` by first reading Section 83.4 (`PR09 — Standard Create: Page-by-Page UI Spec`) in `docs/Operation Management/PACE_ERP_Operation_Management_SAP_Style_Discovery_and_Feasibility.md`, then applying only the two locked backend auth fixes plus the locked Process PO tab rebuild in `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx`.
- Changes: Updated `supabase/functions/api/_acl/route-acl-registry.ts` so `GET /api/production/prodshades` now reuses the same `PROD_PO_CREATE` capability family as `POST /api/production/process-orders` instead of the pack-code master capability. Updated `supabase/functions/api/_core/om/machine.handlers.ts` so `listMachinesHandler()` now uses the repo's existing read-access pattern `assertManagerOrSARole(ctx)` while leaving the SA-only create/update/toggle handlers unchanged. Rebuilt only the Process PO tab in `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` into the locked 3-page flow (Page 1 Company/PO Type/Material, Page 2 Stroke Gate, Page 3 Header + Material Table), removed Notes and the old always-visible full Segment dropdown, auto-derived segment behavior for MTO/HPS/INT plus the MTS-only `IWC`/`POWDER` picker, kept Machine mandatory only for MTO/HPS/MTS/INT, added the locked output-reference block, rebuilt the table to the locked 9 columns, and hard-disabled Create when any line Available is below Standard Qty. Ran `npm.cmd run build` in `frontend/`, and the build passed.
- Notes: Touched files were `supabase/functions/api/_acl/route-acl-registry.ts`, `supabase/functions/api/_core/om/machine.handlers.ts`, `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx`, and `docs/Codex-Log.md`. `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` was the only frontend file changed, and the Packing PO tab block in that file was left untouched. Ambiguities flagged instead of guessed: the locked Page 3 table wants the same registered-alternate Actual Material picker used later in PR11, but the current locked create-page read APIs (`listApprovedProdshades()` and `getStrokeMaster()`) do not expose registered alternate material ids/labels for Standard Create, so I preserved the column shape and same-only combobox behavior without inventing a forbidden backend expansion; Section 83.4 references an MTEST/SKU path, but this repo's current locked create contract still lacks a corresponding unambiguous segment mapping and full MTEST create payload surface in `ProductionPOCreatePage.jsx`, so I did not invent one and recorded that limitation here instead.

### 2026-07-12 13:43 IST
- Task: Fixed the follow-up PR09 Process PO create issues found in live testing: Page 1 material filtering was not honoring selected PO Type, and Page 3 group-based alternate RM/INT rows were not exposing an Actual Material dropdown or substitute-aware stock preview.
- Changes: Updated `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` so Page 1 now derives eligible materials from APPROVED strokes for the selected Company + PO Type instead of using one broad SFG/INT pool, and so Page 3 now builds Actual Material options from both a direct `alternate_material_id` and `material_group.members`, enabling group-based alternates like `SR PCE`. Updated `supabase/functions/api/_core/production/stroke_master.handlers.ts` so `getStrokeMaster()` now enriches each line's `material_group` with resolved member rows/material labels. Updated `supabase/functions/api/_core/production/process_order.handlers.ts` so availability preview and Standard-create validation/prepopulate accept any allowed alternate from the stroke line's direct alternate or alternate-group member set, then stock-check the effective `material_id + storage_location_id` pair accordingly. Re-ran `npm.cmd run build` in `frontend/`, and the build passed.
- Notes: Touched files were `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx`, `supabase/functions/api/_core/production/stroke_master.handlers.ts`, `supabase/functions/api/_core/production/process_order.handlers.ts`, and `docs/Codex-Log.md`. Packing PO remained untouched. Stock preview/save rule after this fix is: if the user selects an Actual Material substitute, preview/save resolve against that selected substitute at the selected/default issue storage location; otherwise they resolve against the formulation material at that same location. No new alternate-design vocabulary was invented; this change only extends the existing direct-alternate path to also honor already-configured `material_group_id` members.

### 2026-07-12 14:02 IST
- Task: Fixed the next live PR09 Process PO create UX/display issues together: slow first-page dropdown readiness, shade-only material labels, Page 2-to-Page 3 bounce-back to Page 1, and bare numeric movement-type display instead of the frozen P-prefixed ERP movement codes.
- Changes: Updated `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx` so single-company users now drive company-scoped queries immediately through an effective company id, Page 1 material labels now render full Prod+Shade context, downstream field edits no longer force `processStep` back to Page 1, and the create-page movement displays now show `P101` / `P261`. Updated `frontend/src/pages/dashboard/production/ProductionPOFinalPage.jsx` and `frontend/src/pages/dashboard/production/ProductionPOVerifyPage.jsx` so the same line/output movement displays now show `P261` / `P101` consistently. Re-ran `npm.cmd run build` in `frontend/`, and the build passed.
- Notes: Touched files were `frontend/src/pages/dashboard/production/ProductionPOCreatePage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOFinalPage.jsx`, `frontend/src/pages/dashboard/production/ProductionPOVerifyPage.jsx`, and `docs/Codex-Log.md`. No backend stock-posting behavior changed in this pass; this was a frontend UX/display correction only, and the chosen source of truth for movement-code formatting was the frozen PACE P-prefix movement-type convention rather than the earlier bare-`261` literal in the PR09 brief text.

### 2026-07-12 14:25 IST
- Task: Implemented `CODEX-GATE27.21-PR10-PROCESS-PO-EDIT-TASK-BRIEF.md` end to end after reading the locked feasibility correction `PR10 (ZCoR2) — Edit Rules — SECOND CORRECTION`, `CLAUDE.md` §8A/§8B/§8C, and the brief-referenced reuse points in `process_order.handlers.ts` / `ProductionPOCreatePage.jsx`.
- Changes: Updated `supabase/functions/api/_core/production/process_order.handlers.ts` so `listProcessOrdersHandler()` now accepts an exact `po_number` filter for PR10 Page 1 lookup, `availabilityPreviewProcessOrderHandler()` adds back the current order's own reservation balance before shortage evaluation in `process_order_id` mode, and `editProcessOrderHandler()` now enforces the new locked PR10 gate (`po_type IN ('MTO','HPS')` and `status = 'STANDARD'` only), accepts header `planned_qty`, recalculates each line's Standard Qty from Dosage% × Batch Qty, syncs reservation `required_qty`, applies per-line storage-location updates to both `process_order_line.issue_sloc_id` and the open reservation, applies the existing cancel-and-reinsert reservation swap when `actual_material_id` changes, and hard-blocks save on shortage with the existing `PROD_PO_INSUFFICIENT_STOCK` 422 shape before any writes. Rebuilt `frontend/src/pages/dashboard/production/ProductionPOEditPage.jsx` into the locked 2-page PR10 flow: Page 1 is PO Number only with `useQuery`-driven lookup, Page 2 shows read-only PO Number / PO Type / Prodshade labels, editable Machine + Batch Qty, the PR09-style material table with live Standard Qty recalculation, per-line Storage Location editing, substitute-only Actual Material comboboxes, inline red shortage preview via the existing availability-preview client, and save payload `{ machine_id, planned_qty, lines: [{ id, storage_location_id, actual_material_id }] }`. This pass also preserves the earlier PR09 fixes by keeping full Prod+Shade labels and the `P261` movement display in the table.
- Notes: Touched files were `supabase/functions/api/_core/production/process_order.handlers.ts`, `frontend/src/pages/dashboard/production/ProductionPOEditPage.jsx`, and `docs/Codex-Log.md`. Verification run: `deno check supabase/functions/api/_core/production/process_order.handlers.ts` still reports only the repo's already-documented shared typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`, with no new handler-local errors; `npm.cmd run build` in `frontend/` passed; repo-wide `npm.cmd run lint -- src/pages/dashboard/production/ProductionPOEditPage.jsx` still surfaces many unrelated pre-existing frontend lint errors elsewhere in the repo, but did not report any `ProductionPOEditPage.jsx` errors in its output; a direct file-only `.\\node_modules\\.bin\\eslint.cmd` attempt hit the pre-existing local Windows `EPERM ... lstat C:\\Users\\cpalm` environment issue instead of a file-specific lint result. Checklist-by-code-path confirmation: QA_APPROVED/BATCH_STARTED are now rejected by the explicit `status !== "STANDARD"` gate, STANDARD MTS/INT/MTEST are rejected by the explicit `po_type` gate, batch-qty edits feed the same Dosage% recalculation into both the frontend display and backend `planned_qty` / reservation `required_qty`, storage-location edits update both line + reservation, substitute validation still returns `PROD_PO_SUBSTITUTE_NOT_REGISTERED`, and shortage remains a hard 422 pre-write stop. Ambiguity flagged instead of guessed: the locked PR10 brief repeatedly narrows Actual Material to only lines with a direct `stroke_line.alternate_material_id`, while this repo's later PR09 create fix already broadened substitute selection to material-group members too; I followed the narrower PR10 brief literally here and did not extend PR10 edit to group-member substitutes.

### 2026-07-12 19:24 IST
- Task: Implemented `CODEX-GATE19.2-OPENING-STOCK-REDESIGN-TASK-BRIEF.md` end to end by reading the locked feasibility section 29.5 first, then applying the IN05 dense-table/currency changes, the backend company-scope rules, and the new IN06 approval/correction flow.
- Changes: Added `supabase/migrations/20260712193000_gate19_2_opening_stock_redesign.sql` to add `erp_procurement.opening_stock_document.currency_code` with default `INR` and register `PROC_OPENING_STOCK_APPROVAL` / tx-code `IN06` under `GRP_ACL_INVENTORY`. Updated `supabase/functions/api/_core/procurement/opening_stock.handlers.ts` to store `currency_code` on create, enforce non-admin company scope on create/approve via resolved `ctx.context.companyId`, expose `getOpeningStockDocumentByNumberHandler()`, and add the batched submitted-document correction save handler `batchUpdateOpeningStockLinesHandler()`. Updated `supabase/functions/api/_routes/procurement.routes.ts` and `supabase/functions/api/_acl/route-acl-registry.ts` for the new by-number and batch-correction routes plus the IN06 approval resource/action wiring. Updated `frontend/src/pages/dashboard/procurement/procurementApi.js`, `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, and `frontend/src/router/AppRouter.jsx` for the new API calls and `OpeningStockApprovalPage.jsx` route/screen. Rebuilt `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockListPage.jsx` so create now resolves company from the current runtime context and uses the locked PO-style `currency_code` select (`INR`/`USD`). Replaced `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx` with the locked IN05 flow using document-currency formatting and a non-paginated plain HTML bulk-entry table inside the required bordered white card. Added `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockApprovalPage.jsx` as the new IN06 two-step screen: document-number lookup on Page 1, then a 25-row paginated editable correction table on Page 2 with local-state edit persistence, batch `Save Corrections`, and approval that reuses the existing approve/post handlers sequentially.
- Notes: Touched files were `supabase/migrations/20260712193000_gate19_2_opening_stock_redesign.sql`, `supabase/functions/api/_core/procurement/opening_stock.handlers.ts`, `supabase/functions/api/_routes/procurement.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/procurement/procurementApi.js`, `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, `frontend/src/router/AppRouter.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockListPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockApprovalPage.jsx`, and `docs/Codex-Log.md`. Verification run: `npm.cmd run build` in `frontend/` passed. `deno check supabase/functions/api/_core/procurement/opening_stock.handlers.ts supabase/functions/api/_routes/procurement.routes.ts supabase/functions/api/_acl/route-acl-registry.ts` still reports only the repo's pre-existing shared typing failures across unrelated procurement/shared files and no longer reports any new opening-stock-local type errors from this pass. Repo-wide `npm.cmd run lint` still reports many pre-existing frontend errors outside this task; the touched opening-stock files only showed memo-dependency warnings during the first pass, which were cleaned before logging. Environment limitation recorded instead of guessed through: this workspace did not provide a live running app/session to directly click-verify IN06 sidebar nesting or the brief's 403 UI flows, so those were verified by route/menu/ACL wiring inspection and code-path review rather than by an interactive browser run. Ambiguity handled without inventing new design: the brief required reuse of the existing approve+post flow but no combined backend approval endpoint exists, so `OpeningStockApprovalPage.jsx` calls the existing approve handler and then the existing post handler sequentially instead of introducing a new backend flow.

### 2026-07-12 22:58 IST
- Task: Implemented only Change 3 and Change 4 of `CODEX-GATE19.2-OPENING-STOCK-REDESIGN-TASK-BRIEF.md` after re-reading feasibility Section 29.5 plus both same-evening addenda and re-applying `CLAUDE.md` §8A/§8B/§8C: Opening Stock batch-number wiring for SFG/FG lines and IN05 Page 1 Material Type + conditional PO Type gating.
- Changes: Added `supabase/migrations/20260712233000_gate19_4_opening_stock_doc_scope.sql` so `erp_procurement.opening_stock_document` is now scoped by `material_type` plus optional `po_type`, with the locked enum/check rules and a new company+cut-off+scope unique index replacing the older company+cut-off-only uniqueness. Updated `supabase/functions/api/_core/procurement/opening_stock.handlers.ts` so opening-stock document create now requires/stores `material_type` and conditional `po_type`, enforces the locked SFG/FG-vs-RM/PM/INT header rules, validates duplicate documents at the new scoped level, blocks line material mismatches against the document header scope, normalizes/stores `opening_stock_line.batch_number` only for SFG/FG materials, preserves null for RM/PM/INT, batches material-type lookups in the submitted-line correction path, and passes `p_batch_number: line.batch_number || null` into the existing `post_stock_movement()` call without changing movement-type selection. Updated `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockListPage.jsx` so IN05 Page 1 now collects Material Type (`RM/PM/INT/SFG/FG`) and shows PO Type (`MTO/HPS/MTS/MTEST`) only when Material Type is `SFG`/`FG`, then sends that header scope in the create payload. Updated `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx` so the material picker is document-type-scoped, the real entry grid remains available for `RM/PM/INT` and `SFG/FG + MTS/MTEST`, the literal placeholder text `Will open after implementation` is shown instead of any entry grid for `SFG/FG + MTO/HPS`, and both single-entry and dense-table entry paths now carry Batch Number only for `SFG/FG` materials while rendering disabled/blank `—` for `RM/PM/INT`; the dense table places Batch Number between Base UOM and Counted Stock and shows the locked help text about MTO/HPS vs MTS. Updated `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockApprovalPage.jsx` so IN06 Page 2 now shows/edits the same Batch Number column with the same SFG/FG-only rule and includes that field in the batch correction save payload. This pass intentionally reused the already-present Dev-applied line migration `supabase/migrations/20260712220000_gate19_3_opening_stock_batch_number.sql` instead of creating a duplicate batch-number line migration.
- Notes: Touched files were `supabase/migrations/20260712233000_gate19_4_opening_stock_doc_scope.sql`, `supabase/functions/api/_core/procurement/opening_stock.handlers.ts`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockListPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockApprovalPage.jsx`, and `docs/Codex-Log.md`. Verification run: `npm.cmd run build` in `frontend/` passed. `deno check supabase/functions/api/_core/procurement/opening_stock.handlers.ts` now falls back only to the repo's already-documented shared typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`, with no new opening-stock-local type errors from this pass. Repo-wide `npm.cmd run lint` still reports many pre-existing frontend errors outside this task, and a direct file-only `.\\node_modules\\.bin\\eslint.cmd` run for the touched opening-stock screens hit the same pre-existing local Windows `EPERM ... lstat C:\\Users\\cpalm` environment issue already seen in earlier sessions instead of returning file-scoped lint results. Verification-item limitation recorded instead of guessed through: this workspace did not provide live DB/browser access to directly execute item 7's `stock_ledger.batch_number` post-check or item 8's click-through routing across all four combinations, so those were verified here by the exact code-path wiring and guards rather than by an interactive Dev run. No additional ambiguity was guessed through in this pass; where the addendum said the line-level batch migration was already applied to Dev, I reused the existing repo migration and limited this pass to the remaining locked Change 3/4 work only.

### 2026-07-12 23:12 IST
- Task: Applied the immediate IN05 follow-up layout correction requested after live review so the page uses the available vertical space properly and keeps the entry controls inside the Opening Stock Lines area instead of dropping them into a separate lower card.
- Changes: Updated `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx` to remove the oversized standalone `Document Header` card, collapse the document metadata into a compact top preview grid, remove the fixed tall empty lines-grid height, and move the `Single entry / Bulk entry` controls into the same `Opening stock lines` section below the existing line list/edit area.
- Notes: Touched files were `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx` and `docs/Codex-Log.md`. Verification run: `npm.cmd run build` in `frontend/` passed after the layout change.

### 2026-07-12 23:28 IST
- Task: Applied the immediate IN05/IN06 follow-up fix for OM rule compliance and IN06 correction usability: no raw storage-location UUID display, plus the ability to add new material rows from IN06 before approval.
- Changes: Updated `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx` so unresolved storage locations in the Opening Stock Lines display now show `--` instead of a raw UUID while keeping resolved OM labels unchanged. Updated `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockApprovalPage.jsx` so unresolved storage locations no longer surface raw UUIDs in IN06, the correction table now supports temporary new rows with `Add Row`, and those unsaved rows participate in pagination, edit tracking, and `Save Corrections`. Updated `supabase/functions/api/_core/procurement/opening_stock.handlers.ts` so `batchUpdateOpeningStockLinesHandler()` now accepts both existing submitted lines and brand-new correction rows in one save, assigning the next line numbers server-side while preserving the same stock-type, quantity, material-scope, and batch-number validation rules.
- Notes: Touched files were `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockDetailPage.jsx`, `frontend/src/pages/dashboard/procurement/opening-stock/OpeningStockApprovalPage.jsx`, `supabase/functions/api/_core/procurement/opening_stock.handlers.ts`, and `docs/Codex-Log.md`. Verification run: `npm.cmd run build` in `frontend/` passed. `deno check supabase/functions/api/_core/procurement/opening_stock.handlers.ts` still falls back only to the repo's already-documented shared typing noise in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`, with no new opening-stock-local checker failures introduced by this pass.
### 2026-07-13 10:55 IST
- Task: Implemented Gate-27.22 Pack BOM full rebuild first, verified it, then implemented Gate-27.23 Packing PO final stock posting rebuild on top of it. No migration was applied and no git push was run.
- Changes: Added `supabase/migrations/20260713093000_gate27_22_pack_bom_full_rebuild.sql` for company-scoped Pack BOMs, Pack BOM line storage/movement/primary-container fields, `pack_code_master.outer_uom_code`, locked pack-code seed values, and SFG Pack BOM line support. Added `supabase/migrations/20260713103000_gate27_23_packing_po_final_stock_posting.sql` to allow Packing PO `FG` lines. Rebuilt `supabase/functions/api/_core/production/pack_bom.handlers.ts` with eligible-SKU lookup, company-scoped list/detail, server-synthesized OUTPUT/SFG/PM lines, F-location and stroke-default SFG location validation, no-BOM auto-activation conversion sync, approval conversion sync, and PR07/PR08 primary-container propagation. Updated `supabase/functions/api/_core/om/material.handlers.ts` to resolve `material_plant_ext.default_storage_location` display data. Rebuilt PR05/PR06 frontend flow in `PackBomCreatePage.jsx` and `PackBomApprovalPage.jsx`, added primary-container fields in `strokeShared.jsx`, and propagated the field through `ChangePackBomPage.jsx` / `ChangePackBomApprovalPage.jsx`. Updated `packing_order.handlers.ts` so Packing PO create now rejects missing ACTIVE Pack BOMs, sources SFG/FG/PM lines from Pack BOM, Final posts SFG P261 OUT + PM P261 OUT + FG P101 IN with shared PO document number and batch number, CORS reverses SFG/PM as P262 and FG as P102, and added minimal COR6-style append-only correction plus FG stock breakdown endpoint. Added `FgStockBreakdownPage.jsx`, production API helpers, route wiring, ACL registry entries, and screen registry entry.
- Files touched: `supabase/migrations/20260713093000_gate27_22_pack_bom_full_rebuild.sql`, `supabase/migrations/20260713103000_gate27_23_packing_po_final_stock_posting.sql`, `supabase/functions/api/_core/production/pack_bom.handlers.ts`, `supabase/functions/api/_core/production/packing_order.handlers.ts`, `supabase/functions/api/_core/om/material.handlers.ts`, `supabase/functions/api/_routes/production.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/PackBomCreatePage.jsx`, `frontend/src/pages/dashboard/production/PackBomApprovalPage.jsx`, `frontend/src/pages/dashboard/production/ChangePackBomPage.jsx`, `frontend/src/pages/dashboard/production/ChangePackBomApprovalPage.jsx`, `frontend/src/pages/dashboard/production/FgStockBreakdownPage.jsx`, `frontend/src/pages/dashboard/production/strokeShared.jsx`, `frontend/src/pages/dashboard/production/prodApi.js`, `frontend/src/router/AppRouter.jsx`, `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Ambiguities / limits: Native Supabase MCP tools were not available in this session, so schema verification used repo migrations/code plus local import/build checks; no live DB query or migration apply was performed. Gate-27.23 requested mirroring an existing Process PO COR6 endpoint, but no COR6/correction handler was found in `process_order.handlers.ts` or production frontend pages, so the minimal `{ lines: [{ id, actual_qty }] }` correction shape was built and logged instead of silently pretending a sibling pattern existed. The new FG stock breakdown route/page is code-wired, but menu/ACL business-data snapshot seeding was not performed because this request required local commit only and no live DB setup/apply step.
- Verification: `npm.cmd run build` in `frontend/` passed. Local backend import smoke with dummy Supabase env passed for `pack_bom.handlers.ts`, `packing_order.handlers.ts`, and `production.routes.ts`. `deno check supabase/functions/api/index.ts` and targeted touched-file checks still fail only on the repo's pre-existing shared typing baseline (`_pipeline/context.ts`, `_shared/canonical_access.ts`, `_shared/serviceRoleClient.ts`, older admin/procurement builder typings, etc.); no touched-file-local syntax/import failure remained after the import smoke.

### 2026-07-13 11:12 IST
- Task: Implemented the updated verification findings in `CODEX-GATE27.22-PACK-BOM-FULL-REBUILD-TASK-BRIEF.md` and `CODEX-GATE27.23-PACKING-PO-FINAL-STOCK-POSTING-TASK-BRIEF.md` after the first-pass `003d48a` review.
- Changes: Added `supabase/migrations/20260713120000_gate27_22_pack_bom_variable_conversion_nullable.sql` and updated `pack_bom.handlers.ts` so BOM-not-required conversion sync writes `conversion_factor: null` with `variable_conversion=true`. Added Pack BOM create company-membership validation via `erp_map.user_companies`, resolved `stroke_master.default_storage_location_id` into `stroke_master.default_storage_location` for eligible-SKU responses, replaced PR05's hand-rolled company select with `TransactionCompanySelector`, and factored the MTO/HPS/MTS/MTEST -> PMTO/PHPS/PMTS/PTEST mapping into `productionTypeLabels.js`. Fixed Packing PO FG line `total_qty` to always be KG (`plannedQtyKg`), hydrated Packing PO line storage-location display data, rebuilt `PackingOrderPage.jsx` into the locked Pack-BOM-driven create/detail/final/correction page, added Packing Orders route/screen wiring, aligned FG stock breakdown ACL to `PROD_FG_STOCK_BREAKDOWN`, and registered `PROD_PACKING_PO_FINAL` + `PROD_FG_STOCK_BREAKDOWN` in Dev menu/ACL snapshots.
- Files touched: `supabase/migrations/20260713120000_gate27_22_pack_bom_variable_conversion_nullable.sql`, `supabase/functions/api/_core/production/pack_bom.handlers.ts`, `supabase/functions/api/_core/production/packing_order.handlers.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/PackBomCreatePage.jsx`, `frontend/src/pages/dashboard/production/PackingOrderPage.jsx`, `frontend/src/pages/dashboard/production/productionTypeLabels.js`, `frontend/src/router/AppRouter.jsx`, `frontend/src/navigation/screens/projects/operationModule/operationScreens.js`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Dev DB verification / direct setup: Native Supabase MCP tools were not exposed, so Codex used the approved Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb`. Verified `material_uom_conversion.conversion_factor` was `NOT NULL`, created the local migration, then directly applied the same Dev DDL because `supabase migration list` showed remote-only Claude-applied versions (`20260713045401`, `20260713050328`, `20260713050338`) that would block safe `supabase db push`; post-check confirmed `is_nullable=YES`. Verified `erp_map.user_companies` has `auth_user_id/company_id`, verified stroke default SLocs resolve to display codes, confirmed `packing_order_line` CHECK allows `FG`, inserted the two new menu/ACL resources into `erp_menu.menu_master`, `acl.menu_master`, `acl.capability_menu_actions`, and `acl.version_capability_menu_actions`, regenerated active ACL/menu snapshots, and confirmed `PROD_PACKING_PO_FINAL` has 108 ACL allow rows / 36 menu snapshot rows while `PROD_FG_STOCK_BREAKDOWN` has 36 / 36.
- Notes / limits: `updatePackingOrderLinesHandler` and the `updatePackingOrderLines()` API wrapper are now dead code for the rebuilt `PackingOrderPage.jsx` because the locked design removes pre-Final manual line editing; they were left in place and logged rather than deleted silently. `ProductionPOCreatePage.jsx`'s older Packing tab was not touched in this pass, honoring the prior PR09 boundary that the Packing PO tab was out of scope for that page. No ambiguity was guessed through.
- Verification: `npm.cmd run build` in `frontend/` passed. File-scoped ESLint passed for `PackBomCreatePage.jsx`, `PackingOrderPage.jsx`, `FgStockBreakdownPage.jsx`, `AppRouter.jsx`, and `operationScreens.js`. Backend import smoke passed for `pack_bom.handlers.ts`, `packing_order.handlers.ts`, `production.routes.ts`, and `route-acl-registry.ts`. `deno check` still reports the documented shared baseline plus an older `pack_config.handlers.ts` count typing issue pulled in through route imports; no touched-file-local import/syntax failure remained.

### 2026-07-13 11:42 IST
- Task: Implemented `CODEX-GATE27.24-PACKING-PO-RESERVATION-ENGINE-TASK-BRIEF.md` exactly as the backend-only Packing PO reservation-engine pass on top of verified Gate-27.22/27.23.
- Changes: Added the Gate-27.24 reservation batch-number migration file to source control, and updated `packing_order.handlers.ts` only. Packing PO create now runs a pre-insert hard-block availability check, with SFG checked by `(material_id, storage_location_id, batch_number)` and PM checked generically by `(material_id, storage_location_id)`, both subtracting open reservations. Create now inserts one `PACKING_PO` reservation row for the SFG line with the parent Process PO batch number and one PM reservation row per PM line with `batch_number` omitted/null. Final now releases SFG/PM reservations to `FULLY_ISSUED` after successful P261 posting; Reverse now cancels only still-open Packing PO reservations; COR6 correction now re-checks SFG batch-level availability for positive SFG quantity deltas before posting.
- Files touched: `supabase/migrations/20260713110000_gate27_24_reservation_batch_number.sql`, `supabase/functions/api/_core/production/packing_order.handlers.ts`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Dev DB verification: Native Supabase MCP tools were not exposed, so Codex used the approved Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb`. Confirmed live DB access, confirmed `reservation_document.batch_number` exists and is nullable, confirmed `reservation_number` has a default sequence even though it is `NOT NULL`, confirmed `source_type` allows `PACKING_PO`, confirmed status vocabulary allows `OPEN/PARTIAL/FULLY_ISSUED/CANCELLED`, confirmed existing reservation rows are currently PROCESS_PO-only with no batch values, confirmed the reservation source index is `(source_type, source_id)`, and confirmed stock ledger has batch-capable production rows for batch-specific availability checks.
- Ambiguities / limits: Packing PO has no locked planned/required-by date field in the brief or current table shape, so new Packing PO reservation inserts leave `required_by_date` to the existing nullable default rather than inventing a date. No live end-to-end create/final/reverse/correct test was run because Dev currently has zero `packing_order` rows and this session did not have an authenticated app/API fixture to create business documents through the real route; direct DB inserts would bypass the handler being tested and were not used.
- Verification: Backend import smoke passed for `packing_order.handlers.ts`. `deno check supabase/functions/api/_core/production/packing_order.handlers.ts` still reports only the documented shared baseline in `_pipeline/context.ts`, `_shared/canonical_access.ts`, and `_shared/serviceRoleClient.ts`, with no touched-file-local import/syntax failure.
### 2026-07-13 15:26 IST
- Task: Fixed the Pack BOM Create PR05 SKU/F-location logic so CMP003 Packing PO setup can proceed with company-mapped FG SKUs and company-defined F storage locations.
- Changes: Updated `supabase/functions/api/_core/production/pack_bom.handlers.ts` so eligible FG SKUs now come from `erp_master.material_company_ext` instead of `material_plant_ext`, prodshade pack-config resolution no longer filters/selects the non-existent `prodshade_pack_config.company_id`, and create validation accepts any active `F*` storage location mapped to the selected company via `storage_location_plant_map`/`storage_location_master`. Updated `frontend/src/pages/dashboard/production/PackBomCreatePage.jsx` so the OUTPUT F-location dropdown uses the existing `listStorageLocations({ company_id, is_active: true })` API and filters to F-location codes, matching the intended Stroke-Master-style SLoc selection behavior.
- Files touched: `supabase/functions/api/_core/production/pack_bom.handlers.ts`, `frontend/src/pages/dashboard/production/PackBomCreatePage.jsx`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Dev DB verification: Used Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb` because native MCP tools were not surfaced. Confirmed CMP003 has 10 active FG SKU company mappings and one active company F-location: `F003 - CONSTRUCTION LIQUID FG STORE`.
- Verification: `npm.cmd run build` in `frontend/` passed. Backend import smoke passed for `pack_bom.handlers.ts` with dummy Supabase env after rerunning outside the sandbox due the known Windows EPERM sandbox issue. No ambiguity was guessed through.

### 2026-07-13 15:41 IST
- Task: Applied the PR05 follow-up usability correction so already-created Pack BOM SKUs no longer remain visible in the Pack BOM Create SKU dropdown.
- Changes: Updated `supabase/functions/api/_core/production/pack_bom.handlers.ts` so `listPackBomEligibleSkusHandler()` excludes SKUs that already have a `DRAFT` or `ACTIVE` Pack BOM for the selected company, matching the existing create-time duplicate guard earlier in the user flow.
- Files touched: `supabase/functions/api/_core/production/pack_bom.handlers.ts`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Dev DB verification: Used Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb`. Confirmed CMP003 has 10 mapped FG SKUs, 6 existing open/active Pack BOMs, and therefore 4 remaining SKUs expected in the create dropdown after the exclusion.
- Verification: `npm.cmd run build` in `frontend/` passed. Backend import smoke passed for `pack_bom.handlers.ts` with dummy Supabase env after rerunning outside the sandbox due the known Windows EPERM sandbox issue.

### 2026-07-13 16:38 IST
- Task: Reworked Packing PO create/final flow to match the newly agreed direct Packing PO design: no Packing approval/verify, Page 1 Company + PO Type + SKU, Page 2 SKU quantity/conversion + FG/SFG/PM lines, then direct create and Final posting.
- Changes: Added `supabase/migrations/20260713163000_gate27_packing_po_direct_create_redesign.sql` and applied the same Dev DDL to allow direct Packing PO creation without a mandatory Process PO link, adding source PO type, SKU quantity, FG/SFG conversion, optional machine, and line-level UOM/movement/alternate/group fields. Rebuilt `createPackingOrderHandler()` so it accepts the new direct payload, resolves Packing PO type from `MTO/HPS/MTS/ZTEST`, requires an ACTIVE company Pack BOM for the selected SKU, validates SFG + PM stock by company/material/storage location before insert, creates FG `P101`, SFG `P261`, and PM `P261` lines, and raises Packing PO reservations for SFG/PM. Updated Final posting to use saved movement/UOM values and no longer require a separate entered actual quantity. Rebuilt `PackingOrderPage.jsx` into the two-page direct create drawer with active-Pack-BOM SKU selection, F-location output selector, SFG conversion, PM-only material rows, dosage-based standard quantity, storage-location dropdowns, Has Alternate, group/member controls, and direct detail/final/reverse views. Added `ZTEST -> PTEST` to the shared production type label map.
- Files touched: `supabase/migrations/20260713163000_gate27_packing_po_direct_create_redesign.sql`, `supabase/functions/api/_core/production/packing_order.handlers.ts`, `frontend/src/pages/dashboard/production/PackingOrderPage.jsx`, `frontend/src/pages/dashboard/production/productionTypeLabels.js`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Dev DB verification: Used Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb`. Confirmed the new `packing_order` columns exist and `process_order_id` is nullable; confirmed the new `packing_order_line` columns exist. The schema change was applied directly to Dev after announcing the target tables and intended DDL, and the migration file is present locally for source-control travel.
- Notes / limits: Packing PO approval and Packing PO verify were intentionally not added because the agreed flow is direct create -> final. Machine remains blank on create as agreed; later Process PO consumption/linking can populate it for MTO/HPS/ZTEST, but that linking mechanism was not invented in this pass. The SKU dropdown is sourced from ACTIVE Pack BOMs because Packing PO creation requires the approved packing structure.
- Verification: `npm.cmd run build` in `frontend/` passed. Backend import smoke passed for `packing_order.handlers.ts` with dummy Supabase env after rerunning outside the sandbox due the known Windows EPERM sandbox issue.

### 2026-07-13 17:10 IST
- Task: Fixed the Packing PO create 422 root cause visibility so stock shortages are shown before submit instead of failing only after `Create Packing PO`.
- Changes: Added `GET /api/production/packing-orders/availability-preview` using the existing Packing PO availability engine, wired its ACL to `PROD_ORDER_LIST:VIEW`, added the frontend API helper, and updated `PackingOrderPage.jsx` Page 2 to show Available and Shortage columns for SFG/PM lines. Create is now disabled while stock check is loading or when any selected SFG/PM storage location is short.
- Files touched: `supabase/functions/api/_core/production/packing_order.handlers.ts`, `supabase/functions/api/_routes/production.routes.ts`, `supabase/functions/api/_acl/route-acl-registry.ts`, `frontend/src/pages/dashboard/production/prodApi.js`, `frontend/src/pages/dashboard/production/PackingOrderPage.jsx`, and `docs/Codex-Log.md`.
- Dev DB verification: Used Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb` for read-only root-cause checks. For the failing screen data, `SFG-00004` at `S003` had sufficient ledger stock, `PM-00004` at `P003` had sufficient ledger stock, but `PM-00020` and `PM-00013` at `P003` had zero ledger stock against requirements of 44 and 22 respectively; `PM-00013` had stock at `P004`, confirming the 422 was a real selected-SLoc shortage rather than a backend crash.
- Verification: `npm.cmd run build` in `frontend/` passed. Backend import smoke passed for `packing_order.handlers.ts`, `production.routes.ts`, and `route-acl-registry.ts` after rerunning outside the sandbox due the known Windows EPERM sandbox issue.

### 2026-07-13 17:33 IST
- Task: Fixed the follow-up Packing PO create 500 root cause after stock shortages were cleared.
- Changes: Added `supabase/migrations/20260713172000_reload_postgrest_after_packing_po_direct_create.sql` to reload PostgREST schema cache after the direct-create column migration, and directly issued the same Dev schema reload. Updated `createPackingOrderHandler()` so line-insert/reservation-insert failures are logged with the real backend error and the newly-created Packing PO header is cleaned up instead of leaving a line-less ghost PO.
- Files touched: `supabase/migrations/20260713172000_reload_postgrest_after_packing_po_direct_create.sql`, `supabase/functions/api/_core/production/packing_order.handlers.ts`, `docs/Codex-Log.md`, and `docs/Operation Management/implementation-specs/OM-IMPLEMENTATION-LOG.md`.
- Dev DB verification / cleanup: Used Supabase Management API fallback against Dev `ytapuwiqicmvpanmzelb`. Confirmed the failed 500 attempts had created `packing_order` headers `940001` and `940002` with zero `packing_order_line` rows, proving the failure happened after header insert and before/at line insert. Pushed the reload migration, directly reloaded PostgREST schema cache, and deleted only those two line-less failed headers using a guarded `DELETE` that required no child lines.
- Verification: `npm.cmd run build` in `frontend/` passed. Backend import smoke passed for `packing_order.handlers.ts` after rerunning outside the sandbox due the known Windows EPERM sandbox issue.
